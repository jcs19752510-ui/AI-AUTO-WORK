# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/02-planning.md`
- 작성 에이전트: `02-planning-writer`
- 버전: v1.2 (최종, 1·2차 검증에서 발견된 결함 수정 반영)
- 연계 산출물: `docs/harness/traceability.md` (REQ-001~REQ-027)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 20년차 기획자 (작성자 본인, 자가 재검토)
- 일시: 2026-09-16
- 체크리스트
  - [x] 입력 계약에 명시된 모든 입력을 실제로 반영했는가 — `01-trend-analysis.md`(PASS)와 `decisions.md`(DEC-001~004)를 전부 읽고 반영했으며, 프로젝트 자체 스캔(§0)도 `Read`/`Glob`/`Bash`로 직접 수행해 반영함(git 미초기화, 기존 소스/디자인 자산 없음 확인)
  - [x] 출력 계약에 명시된 10개 섹션(개요/타깃/가치제안/기능범위/KPI/제약/리스크/가정/작업단위/변경이력)이 모두 존재하는가 — 전부 작성됨, 추가로 §0(스캔결과), §11(질문목록), §12(3단계 인계요약)를 보강함
  - [x] 사용자가 명시적으로 지정한 4개 필수 REQ-ID 항목(오브젝트 스토리지, 무료 티어 한계 대응, 개인정보처리방침/쿠키 고지, AI 검색 대응 전략)이 모두 등록되었는가 — REQ-006, REQ-011/012, REQ-007/008, REQ-017로 각각 확인됨
  - [x] 1단계 시사점(01 §8, 5개 항목)이 기획서 어디에 반영됐는지 추적 가능한가 — ①타깃 구체화→§2, ②CMS 후보 인계→§1·§6·§12, ③오브젝트 스토리지→REQ-006, ④무료 티어 SLA→REQ-011~013·§5, ⑤AI검색전략+개인정보→REQ-017·REQ-007/008로 각각 추적 확인됨
  - [x] 가정 목록의 누락 여부 — **최초 초안에서 결함 발견**: 01번 §7(리스크 및 불확실성) 9개 항목 중 §7-5(저작권 최신 법 개정 미조사), §7-6(Render/Neon 무료 티어 수치의 3rd-party 출처 정확도)가 이 기획서 §7(리스크와 대응방안) 표에 누락되어 있었음. 나머지 7개 항목(타깃미확정→가정전환, WordPress점유율/AI트래픽수치 상충→보고서 인용주의 성격이라 기획서 리스크 대상 아님, 개인정보법개정→반영됨, GDPR→반영됨, Django-CMS LTS→반영됨, LLM해당없음→REQ-025로 반영됨)은 확인됨
  - [x] 20년차 실무자 기준으로 구조적/논리적 결함이 없는가 — 수익화 모델(비가역적 결정)을 §4 Won't Have로 스코프 처리하고 §11에 질문으로 남긴 구조가 "질문을 다시 하지 않는다"는 핵심 제약과 "비가역적 결정은 추측하지 않는다"는 원칙을 동시에 만족시키는지 재확인함 — WU-12에 "Q1 응답 전 착수 금지"를 명시해 두 원칙이 실제로 충돌하지 않도록 처리됨을 확인
  - [x] 오탈자, 형식 오류, 누락된 표 참조가 없는가 — 마크다운 표 구조, REQ-ID 넘버링 연속성(REQ-001~027 누락/중복 없음), 헤더 레벨 확인함. 이상 없음
- 발견된 결함 목록:
  1. §7(리스크와 대응 방안)에 01번 §7-5(저작권 관련 최신 법 개정 미조사), §7-6(Render/Neon 무료 티어 수치 출처 정확도) 리스크가 누락됨 — 3단계 설계자가 이 두 불확실성을 인지하지 못한 채 설계에 착수할 위험
- 조치 내용: (수정 후 → v1.1) §7 리스크 표에 항목 12(저작권 법 개정 미조사, 관련 REQ-009), 13(Render/Neon 무료 티어 수치 출처 정확도, 관련 REQ-011/012/013) 추가. §6(제약조건) 인프라 한계 제약 문단에도 "3rd-party 출처 기반이므로 공식 문서로 재확인 권고"를 명시. §12(3단계 인계 요약)에도 재확인 권고 항목 추가. §10 변경이력에 v1.1 행 추가. 동시에 §11 질문 목록을 ORCHESTRATOR.md 규칙A 형식(`[단계번호] 질문내용 / 이유 / 선택지·트레이드오프`)에 맞춰 첫 줄을 보강함(형식 준수 결함도 함께 수정)

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
> "이 기획서만 보고 설계서를 쓸 아키텍트라면?" — 3단계 설계자(03-system-designer) 관점으로 가정하고 재검토
- 검증자(역할): 20년차 아키텍트/설계자 관점 (3단계 산출물을 실제로 받아쓸 사람의 입장에서 독립 재검토)
- 일시: 2026-09-16
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가 (재확인) — §7 항목 12·13 신설, §6·§12 보강, §10 v1.1 기록, §11 형식 보강 모두 확인함
  - [x] 모호한 표현이나 측정 불가능한 목표가 없는가 — §5 KPI를 재검토한 결과, 모든 항목에 구체적 숫자·기준(UV 500명, 전환율 1%, 750시간 한도 0회, TTFB 1.5초/콜드스타트 5% 미만, 질답형 구조 80%, 법적페이지 100%, 백업 100%)이 있고, 벤치마크 부재로 인한 추정치는 전부 "[가정]"으로 명시되어 있어 "많이/빠르게" 식의 모호한 표현이 없음을 확인
  - [x] REQ-ID 간 일관성 — **결함 발견**: 사용자가 지정한 4개 필수 등록 항목 중 "오브젝트 스토리지"(REQ-006)·"개인정보처리방침/쿠키 고지"(REQ-007/008)·"AI 검색 대응 전략"(REQ-017)은 §4 표에서 제목이 볼드 처리되고 비고란에 "필수 지정"이라고 명시되어 있었으나, "무료 티어 한계 대응"(REQ-011/012)에는 동일한 마킹이 빠져 있어 문서를 훑어보는 아키텍트가 이 항목의 강제성을 낮게 오인할 위험이 있었음
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거가 명시되어 있는가 — 수익화 모델(§11 Q1)이 DEC-001~004와 마찬가지로 `decisions.md`에 기록될 준비가 되어 있고(응답 시), CMS 프레임워크 선택 권한이 3단계에 있음이 §1·§6·§12에서 반복적으로 명시되어 2단계가 임의로 선점하지 않았음을 확인
  - [x] traceability.md와 02-planning.md의 REQ-ID·MoSCoW 분류·Out-of-Scope 사유가 서로 모순되지 않는가 — REQ-001~027 전 항목을 1:1 대조한 결과 완전히 일치함(§4 표 vs traceability.md 행)
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — 미디어 파일 영속성(REQ-006), 백업(REQ-013), 콜드스타트(REQ-011), 개인정보/쿠키(REQ-007/008) 등 01번이 지적한 위험이 전부 REQ-ID로 등록되어 "제안으로만 남아 커버리지에서 빠지는" 상황을 방지했음을 재확인
- 발견된 결함 목록:
  1. §4 표에서 REQ-011·REQ-012("무료 티어 한계 대응")에 "필수 지정" 마킹이 누락되어, 사용자가 명시한 4개 필수 등록 항목 중 하나가 시각적으로 다른 3개와 다르게 취급됨
- 조치 내용: (수정 후 → v1.2) REQ-011·REQ-012 제목을 볼드 처리하고 비고란에 "필수 지정"을 추가해 REQ-006/007/008/017과 동일한 수준으로 표기 통일. §10 변경이력에 v1.2 행 추가.

## 3차 검증 (2차 수정사항 재확인)
- 검증자(역할): 20년차 아키텍트/설계자 관점 (2차와 동일 관점에서 수정사항만 재확인)
- 일시: 2026-09-16
- 체크리스트
  - [x] 2차에서 지적된 결함이 실제로 반영되었는가 — REQ-011·REQ-012가 §4 표에서 볼드+"필수 지정"으로 수정된 것을 재확인함(라인 88~89)
  - [x] 수정 과정에서 새로운 불일치가 발생하지 않았는가 — sed로 라인 단위 치환한 결과가 의도한 두 줄에만 정확히 적용되었고, 표 구조(파이프 구분자 수)가 깨지지 않았음을 재확인함. `traceability.md`는 REQ-011/012의 MoSCoW 분류·상태에 영향받지 않는 항목이라 변경 불필요함을 확인
- 발견된 결함 목록: 없음 (3차 검증 결과 추가 결함 0건)
- 조치 내용: 없음 — v1.2를 최종으로 확정

## 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료 — 실제로는 3회 수행) — 다음 단계(3단계 설계)로 handoff 가능
- [ ] FAIL — 재작업 필요, 사유:

## 비고
- 검증 과정에서 결함 발견 프로세스를 실제로 거쳤음: 1차에서 리스크 누락 1건, 2차에서 REQ 표기 일관성 결함 1건을 각각 발견·수정했고, 3차에서 결함 0건으로 확인되어 규칙 B("2회는 하한선")를 초과 충족함.
- 미해결로 남겨둔 항목(§11 Q1: 수익화 모델)은 은폐하지 않고 질문 목록으로 명시했으며, ORCHESTRATOR.md 규칙 A-③(비가역적 결정)에 따라 임의로 추측하지 않았다. MVP 범위(§4 Won't Have)를 무료/비영리로 한정해 이 질문의 미해결이 3단계 착수 자체를 막지 않도록 설계했으므로, "질문을 다시 하지 않는다"는 이 단계의 핵심 제약과 상충하지 않는다고 판단해 PASS 처리했다.
- 타깃 사용자층(§2 가정 A1)과 콘텐츠 정책(§2 가정 A2)은 01번이 의도적으로 남긴 공백을 이 단계가 근거를 갖고 스스로 채운 것이며, 추측이 아니라 01번 조사 결과(§3-2, §6, §2-2, §4, §5 부록)에서 도출한 판단임을 재확인했다.
