# 테스트 결과서 (Test Result Report)

## 1. 개요
- 테스트 대상: 콘텐츠 파일럿(바이브코딩 콘텐츠 플랜 5개 축 20개 글) 실제 게시 + 댓글 모더레이션 플로우 + 관리자 화면 조작
- 테스트 유형: 시스템(수동 E2E, 실제 브라우저 조작 1건 + 백엔드 시딩 19건)
- 적용 Tier: Low (로컬 검증용 콘텐츠 파일럿, 프로덕션 배포 아님)
- 적용 속도 트랙: N/A (콘텐츠 운영 검증이며 코드 변경 없음)
- 테스트 목적: (1) Wagtail 어드민에서 실제로 글을 작성·발행하는 전체 절차가 "바이브코딩 콘텐츠 플랜" 아티팩트와 어긋남 없이 동작하는지 확인 (2) 댓글 작성→승인 대기→운영자 승인→프런트 노출 전체 루프 확인 (3) 카테고리·태그·FAQ 구조화 데이터·RSS·사이트맵이 신규 카테고리 5개에도 정상 반영되는지 확인
- 관련 산출물: `바이브코딩 콘텐츠 플랜` 아티팩트(2026-09-26 게시), `webapp/CONTENT_GUIDE.md`, `docs/harness/11-admin-manual.md` §2.6
- 테스트 수행자: Claude (세션 에이전트, 사용자 실시간 참관)
- 테스트 일시: 2026-09-26

## 2. 테스트 범위 및 제외 범위
- 범위(In-Scope):
  - 카테고리 5개 신설(바이브코딩 입문/사장님 자동화/사무직 자동화/나만의 도구 만들기/실전사례·FAQ)
  - 블로그 글 20개 작성·발행(1편은 실제 브라우저 UI로 처음부터 끝까지 직접 작성, 19편은 동일한 데이터 구조로 백엔드 시딩 — 사용자 명시적 선택)
  - 방문자 댓글 작성 1건(실제 브라우저 제출) + 데모용 샘플 댓글 9건(다양한 상태 재현 목적)
  - 관리자 화면: 로그인, 글 작성/카테고리·태그 선택/FAQ 블록 편집/발행, 댓글 모더레이션(승인)
  - 프런트엔드: 홈 목록, 게시물 상세, FAQ 렌더링, RSS, 사이트맵, 태그 페이지, 존재하지 않는 카테고리 404
- 제외 범위 및 사유:
  - 실제 유튜브 영상 링크 삽입: 사용자로부터 실제 영상 URL을 받지 못해 임의로 링크를 지어내지 않음(추측 금지 원칙). **미해결 항목으로 8절에 남김.**
  - 실제 이미지(대표 이미지/본문 이미지) 삽입: 저작권 있는 실제 사진 자산이 없어 텍스트 콘텐츠만 검증. 이미지 업로드 자체의 기능 동작은 기존 08단계에서 이미 PASS 확인됨(별도 재검증 불필요).
  - 애드센스 연동, 프로덕션 배포: 별도 대화에서 "콘텐츠 준비 우선" 결정에 따라 이번 라운드 범위 밖.

## 3. 테스트 환경
- 실행 환경: Windows 11, 로컬 개발 서버(`python manage.py runserver 127.0.0.1:8010`), SQLite(`db.sqlite3`), DEBUG=True
- 브라우저 자동화: Playwright(Chromium), CDP 연결 방식(claude-in-chrome 확장 미연결로 대체 사용)
- 테스트 데이터: 신규 카테고리 5개, 신규 블로그 글 20개, 신규 댓글 10건(방문자 실제 제출 1건 포함), 관리자 계정 `admin`(로컬 전용 비밀번호, 이전 대화에서 별도 안내)
- 전제 조건: 웹 서버 기동 상태, DB 마이그레이션 최신(`showmigrations`로 사전 확인 완료 — 이전 세션)

## 4. 테스트 케이스 및 결과
| ID | 시나리오 | 사전조건 | 실행 절차 | 예상 결과 | 실제 결과 | Pass/Fail | 비고 |
|----|----------|----------|-----------|-----------|-----------|-----------|------|
| TC-001 | 신규 카테고리 5개 생성 | 관리자 세션 | Django shell로 Category 생성 | 5개 카테고리 정상 저장, 슬러그 자동 생성 | 생성됨. 1개(바이브코딩 입문) 슬러그가 최초 오타(메이로-입문)로 잘못 생성되어 수동 정정 | Pass(수정 후) | DEF-001 참고 |
| TC-002 | 브라우저로 글 1편 실제 작성 | 관리자 로그인, 카테고리 준비됨 | cms-admin 로그인→새 글→제목/Intro/카테고리/태그/문단/FAQ 입력→게시 | "게시됨" 배너, 라이브 상태 전환 | 정상 게시 확인(스크린샷 다수) | Pass | |
| TC-003 | 프런트엔드에서 신규 글 노출 확인 | TC-002 완료 | 홈 접속, 상단 메뉴·카드 확인 | 새 카테고리가 메뉴에, 새 글이 카드에 노출 | 정상 노출 확인 | Pass | |
| TC-004 | FAQ 블록 구조화 데이터 렌더링 | TC-002 완료 | 게시물 상세 HTML에서 `FAQPage` 문자열 검색 | JSON-LD에 FAQPage 포함 | 포함 확인(2개 글 스팟체크) | Pass | |
| TC-005 | 방문자 댓글 제출(무-JS 전용 페이지) | 발행된 글 존재 | `/blog/<slug>/comment/`에서 이름·내용 입력 후 제출 | "댓글이 접수되었습니다. 검토 후 게시됩니다." 표시, status=pending 저장 | 정상 확인 | Pass | |
| TC-006 | 댓글 모더레이션(승인) | TC-005 완료 | `/cms-admin/snippets/comments/comment/`에서 status를 승인됨으로 변경·저장 | 수정 성공 배너 표시 | 정상 확인 | Pass | |
| TC-007 | 승인된 댓글의 프런트 반영 지연(캐시) | TC-006 직후 | 같은 게시물 페이지 새로고침 | 최대 10분 캐시로 즉시 반영 안 될 수 있음(관리자 매뉴얼 §2.6 기존 문서화 내용) | 문서화된 대로 즉시 미반영 재현 — 오류 아님 | Pass(설계된 동작 재확인) | |
| TC-008 | 나머지 19편 백엔드 시딩 | 카테고리 준비됨, 사용자가 "빠른 방식" 선택 | Django ORM으로 BlogPostPage 19건 생성→`save_revision().publish()` | 19건 모두 live=True | 19/19 생성·발행 확인(제목 목록 UTF-8 파일로 대조) | Pass | |
| TC-009 | 카테고리별 게시물 수 정합성 | TC-008 완료 | 카테고리별 `BlogPostPage.objects.filter(category=c, live=True).count()` | 입문5/사장님4/사무직4/도구3/사례4/여행1 = 21 | 실측 일치 | Pass | |
| TC-010 | RSS/사이트맵 반영 | 20개 글 발행 완료 | `/feed.xml`, `/sitemap.xml` 조회 후 `<item>`/`<loc>` 개수 집계 | 사이트맵 25건(홈1+법적3+게시물21), RSS 최대 20건 | 사이트맵 25건 확인, RSS 20건(피드 기본 노출 상한, 최신순) 확인 | Pass | |
| TC-011 | 태그 페이지 | 태그 "바이브코딩" 존재 | `/tag/바이브코딩/` 접속 | 200 응답 | 200 확인 | Pass | |
| TC-012 | 존재하지 않는 카테고리 404 | - | `/category/bogus-xyz/` 접속 | 404 응답 | 404 확인 | Pass | |
| TC-013 | 데모용 샘플 댓글 9건(승인/대기/거부 혼합) | 게시물 다수 존재 | Django shell로 다양한 status의 Comment 생성 | 스니펫 목록에 9건 추가, 상태별 정상 구분 | 정상 확인(스크린샷) | Pass | |

## 5. 커버리지
- 커버리지 지표: 콘텐츠 플랜 5개 축 전부 최소 1편 이상 실제 발행 완료(입문5/사장님4/사무직4/도구3/사례4). 댓글 상태 3종(승인/대기/거부) 전부 실제 데이터로 재현.
- 커버되지 않은 부분과 사유: 대표 이미지·본문 이미지·실제 영상 링크는 실제 자산이 없어 텍스트 콘텐츠만 검증(2절 제외 범위 참고). 예약발행(go_live_at)·리비전 복원은 08단계에서 이미 PASS 확인된 기존 기능이라 이번 라운드에서 재검증하지 않음.

## 6. 결함(Defect) 목록
| ID | 설명 | 재현 절차 | 심각도 | 상태 | 조치 내용 |
|----|------|-----------|--------|------|-----------|
| DEF-001 | "바이브코딩 입문" 카테고리 최초 생성 시 오타로 "메이로 입문"으로 저장되고 슬러그도 그 값 기준으로 생성됨 | 유니코드 이스케이프 수기 작성 중 실수 | Low | Fixed | 이름·슬러그 재저장으로 정정, 화면 재확인 완료 |
| DEF-002 | 테스트 목적으로 세션 초반 curl로 넣은 댓글(id=2)이 터미널 콘솔 인코딩(cp949) 문제로 저장 시 mojibake 발생 | 관리자 화면 댓글 목록에서 깨진 문자 확인 | Low | Fixed | Django shell로 author_name/body를 정상 UTF-8 값으로 재저장 |
| DEF-003 | 요청된 "동영상 링크 삽입"이 실제로 반영되지 않음 | 20개 글 어디에도 유튜브 링크 없음 | Medium(요청사항 미이행) | **Fixed** (2026-09-26 추가 조치) | 사용자로부터 실제 영상 URL(`youtube.com/watch?v=58YFu8s2I8w`, "2026 클로드 올인원 무료 강의｜AI 업무자동화 20분 총정리") 전달받음 → 사용자 선택("20편 전체에 하단 참고 영상으로")에 따라 8~27번 페이지 본문 끝에 문단 블록으로 링크 추가 후 재발행. `curl`로 게시물 HTML에 해당 URL 문자열 포함 확인(TC-014) |

- TC-001~TC-013은 실제 조작/조회로 실측 확인(추정 아님). 아래 TC-014로 DEF-003 조치 결과도 실측 확인 완료.

| TC-014 | 20개 글 전체에 참고 영상 링크 추가 | DEF-003 조치 | Django ORM으로 각 페이지 body에 링크 문단 append → `save_revision().publish()` | 20/20 갱신, 프런트에 링크 노출 | 20/20 업데이트 로그 확인 + 대표 1건 `curl`로 URL 문자열 렌더링 확인 | Pass | 브라우저 시각 재확인은 자동화 브라우저 프로세스 종료로 생략(HTML 응답으로 대체 확인) |

## 7. 테스트 환경 정리(Teardown) 확인 — 규칙 K
- 이번 테스트에서 생성한 임시 아티팩트 목록:
  - 스크린샷/스크립트 다수: `C:\Users\jcs19\AppData\Local\Temp\claude\...\scratchpad\`(세션 스크래치패드, 프로젝트 저장소 밖)
  - Playwright 브라우저 프로세스(CDP 9222) — 세션 종료 시 함께 종료됨
  - **DB에 실제로 남기는 데이터**(임시 아티팩트 아님, 의도된 산출물): 카테고리 5개, 블로그 글 20개, 댓글 10건, `db.sqlite3`는 `.gitignore` 처리되어 커밋되지 않음
- 위 아티팩트를 전부 `.harness-tmp/` 하위에서만 생성했는가: 아니오 — 스크립트/스크린샷은 프로젝트 저장소 밖의 세션 스크래치패드에 생성했으며, 프로젝트 내부에는 이번 라운드에서 어떤 임시 파일도 만들지 않았다(사유: 콘텐츠 시딩 스크립트는 1회성이며 커밋 대상 코드가 아니므로 `.harness-tmp/`보다 세션 스크래치패드가 더 적절하다고 판단).
- 정리(삭제) 완료 여부: 세션 스크래치패드는 세션 종료 시 자동 정리 대상이며 프로젝트 저장소에는 영향 없음. 프로젝트 루트에 실수로 남겼던 `cat_check.txt`, `extract.txt`는 발견 즉시 삭제 완료(이전 라운드).
- 정리 후 `git status --short` 실행 결과(그대로 첨부):
  ```
  ?? docs/harness/test-report_content-pilot-20articles.md
  ```
  (이 결과서 파일 자체 외에는 변경분 없음 — `webapp/` 소스 코드 변경 없이 DB 데이터만 시딩했으므로 정상)
- 이번 테스트 도중 강제 중단(TaskStop 등)이 있었는가: 있음 — 로컬 서버가 시스템 메모리 부족으로 2회 자동 종료되었으나(백그라운드 셸 압박 회수), 콘텐츠 시딩 작업 자체와는 무관하며 재기동 후 데이터 정합성 재확인 완료.

## 8. 리스크 및 잔존 이슈
- **영상 1편을 20개 글 전체에 동일하게 삽입**: 사용자가 "20편 전체에 하단 참고 영상으로" 넣어달라고 명시적으로 선택했다. 영상이 실제로는 입문/종합 강의 성격이라 사무직·사장님 자동화 등 구체적인 실무 글과는 주제 결이 다를 수 있다 — 추후 실제 방문자 반응을 보고 글별로 더 맞는 개별 영상으로 교체할지 재검토 권장.
- **대표 이미지 미등록**: 20개 글 모두 대표 이미지가 없는 상태로 발행됨. 실제 SNS 공유 카드에는 텍스트만 노출된다(설계상 정상 동작이나, 실제 운영 전 이미지 등록 권장).
- **로컬 전용 데이터**: 이번에 만든 카테고리/글/댓글은 로컬 SQLite에만 존재하며 프로덕션 배포 시 별도로 다시 작성하거나 마이그레이션 절차가 필요하다.

## 9. 결론 및 판정
- [x] PASS — 다음 단계 진행 가능. DEF-001~DEF-003 전부 Fixed로 종결(7절 Teardown 확인 완료 전제). 20개 글 발행·댓글 모더레이션·구조화 데이터·RSS/사이트맵 반영·참고 영상 삽입까지 전부 실측으로 확인됨.

## 10. 내부 검증 (최소 2회)

### 1차 검증 (작성자 관점 자가 재검토)
- 검증자: Claude(작성 에이전트, 자가 재검토)
- 일시: 2026-09-26
- 체크리스트
  - [x] 입력 계약(콘텐츠 플랜 5개 축 20개 글감)에 명시된 모든 글을 실제로 반영했는가 — 예, 20/20 확인
  - [x] 출력 계약(카테고리·태그·FAQ 포함 발행)이 빠짐없이 존재하는가 — 예
  - [x] 상위 산출물(콘텐츠 플랜 아티팩트)과 제목·카테고리 매핑이 모순되지 않는가 — 예, 1:1 대조 완료
  - [x] 추측으로 채운 항목이 있는가 — 있음(영상 링크는 넣지 않음, 이미지도 미등록) → 8절에 리스크로 명시했고 지어내지 않았음
  - [x] 20년차 실무자 기준 구조적 결함 — 카테고리 오타(DEF-001) 1건 발견 및 즉시 수정
  - [x] 오탈자/인코딩 오류 — 댓글 mojibake(DEF-002) 1건 발견 및 즉시 수정
- 발견된 결함 목록: DEF-001, DEF-002
- 조치 내용: 둘 다 즉시 수정 후 재확인 → v1

### 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
- 검증자: Claude(동일 세션, "오늘 처음 이 결과를 인계받는 심사자" 관점으로 역할 전환)
- 일시: 2026-09-26
- 체크리스트
  - [x] 1차 검증 지적 항목이 실제로 반영되었는가 — 재확인 완료(카테고리명·댓글 내용 정상 표시 스크린샷/파일로 확인)
  - [x] 엣지 케이스 누락 여부 — 댓글 3가지 상태(승인/대기/거부) 모두 재현, 존재하지 않는 카테고리 404 확인, 태그 페이지 확인 — 누락 없음
  - [x] 문서만 보고 다음 사람이 추가 질문 없이 이어받을 수 있는가 — 영상 링크 미반영은 "다음 필요 입력"으로 명확히 남겨 이어받기 가능
  - [x] 되돌리기 어려운 결정 근거 — 로컬 전용 데이터이며 프로덕션에 영향 없음(가역적)
  - [x] 보안/운영 관점 위험 — 없음(로컬 SQLite, 관리자 비밀번호는 채팅으로만 별도 전달, 문서/PDF에는 미기재 원칙 유지)
- 발견된 결함 목록: 없음
- 조치 내용: 해당 없음(2차에서 신규 결함 없음)

## 최종 판정
- [x] CONDITIONAL PASS (결함 0건 도달, 단 DEF-003 정보 부족 항목 1건 Open) — 사용자가 실제 영상 URL을 전달하면 즉시 반영 가능한 상태
