# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/11-admin-manual.md`
- 작성 에이전트: `11-documentation-writer`
- 적용 Tier: Standard (08/09단계와 동일하게 이 프로젝트 전체가 Standard Tier로 선언됨 — `09-security-audit.md` §1, DEC-036. 최소 2회 검증 필수, 1차 결함 0건이어도 2차 생략 불가)
- 버전: v1 (1차 검증에서 발견된 결함 조치 반영본)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 문서 작성 에이전트 본인(20년차 테크니컬 라이터 페르소나, 작성 직후 자가 재검토)
- 일시: 2026-09-25
- 체크리스트
  - [x] 입력 계약에 명시된 모든 입력(03-system-design.md §5.1/§5.6/§4, 04-ux-design.md §1.2, 08-full-system-test.md §4-4, 09-security-audit.md 최신 판정, ADMIN_ACCESS_GUIDE.md, admin_auth.py, subscribers/admin.py)을 실제로 반영했는가 — 반영 확인. 관리자 진입점 2개(`/cms-admin/`, `/django-admin/`) 구조, 공유 레이트리밋 카운터, Editors/Moderators 권한 표, 구독자 하드삭제 절차 전부 원문 대조 완료
  - [x] 출력 계약(권한체계/관리기능별 사용법/되돌리기 어려운 작업/레이트리밋 429)에 명시된 모든 필수 항목이 빠짐없이 존재하는가 — §1~§5 구조로 전부 커버
  - [x] 상위 단계 산출물과 용어/수치/범위가 모순되지 않는가 — 레이트리밋 "IP당 15분 10회, 공유 카운터"는 03 §5.1/admin_auth.py/ADMIN_ACCESS_GUIDE.md 3곳 모두와 정확히 일치. 09단계 최종 판정(PASS, DEF-09-01 Fixed)을 정확히 인용(과거 FAIL이었다가 재작업 후 PASS로 갱신되었다는 맥락을 왜곡 없이 반영)
  - [x] 추측으로 채운 항목이 있는가 — 초안에서 "카테고리 삭제 시 참조 게시물이 어떻게 되는지 불확실"이라고만 썼던 부분을 발견해 `webapp/blog/models.py`를 직접 확인, `category` 필드가 `on_delete=models.PROTECT`임을 코드로 확정해 v1에서 정확한 사실로 교체(추측 제거). 그 외 추측으로 채운 항목 없음
  - [x] 20년차 실무자 기준 구조적/논리적 결함 없는가 — 있음(1건, 아래 발견된 결함 참고), 조치 완료
  - [x] 오탈자/형식 오류/누락된 표 참조 없음 확인
- 발견된 결함 목록:
  1. §2.2(카테고리 관리)에서 카테고리 삭제 시 기존 게시물에 미치는 영향을 "실측 확인된 바 없음"이라고 모호하게 서술함 — 실제로는 `webapp/blog/models.py`의 `BlogPostPage.category` 필드가 `on_delete=models.PROTECT`로 선언되어 있어 참조 중인 카테고리는 삭제가 원천 차단되는 것이 코드 사실로 확인 가능했는데도, 입력 목록에 명시된 파일만 확인하고 모호하게 넘어갔던 것이 결함이었다.
- 조치 내용: (수정 후 → v1) `webapp/blog/models.py`를 추가로 열람해 `on_delete=PROTECT` 사실을 확인하고, §2.2 해당 문단을 "삭제가 구조적으로 차단된다"는 정확한 사실로 교체. 문서 상단 입력 목록과 하단 "검증 근거 요약"에도 이 파일과 근거를 추가해 출처를 명시함.

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
> "이 문서 하나만 받고 오늘 처음 이 서비스의 관리자가 된 사람"의 관점에서, 1차 검증자(작성자 본인)가 놓쳤을 법한 리스크·엣지케이스·암묵적 가정을 의심하며 재검토했다.
- 검증자(역할): 독립 심사자 관점(신규 부임 관리자 페르소나로 역할 전환)
- 일시: 2026-09-25
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가 — §2.2 재확인, `on_delete=PROTECT` 사실이 정확히 반영됨
  - [x] 엣지 케이스/예외 상황이 누락되지 않았는가 — 점검한 엣지케이스: (a) Editors 그룹이 발행 버튼을 못 보는 경우 → §2.1 말미에 "오류가 아니라 의도된 구조"로 명시되어 있어 신규 관리자가 당황하지 않도록 처리됨. (b) 구독자 목록에서 다건 삭제 시 "전체 선택" 오조작 → §3.2에 명시적 금지·절차로 반영됨. (c) 429가 왜 다른 화면에서도 걸리는지(카운터 공유 이유) → §4.2에서 원리까지 설명해 신규 관리자가 "버그 아닌가"라고 오판하지 않도록 함. (d) `status`가 "구독중"으로 보여도 실제 수신 의사를 보장하지 않는다는 함정 → §2.5에서 명시적으로 경고함. 누락된 엣지케이스를 추가로 찾지 못함
  - [x] 문서만 보고 다음 단계(신규 관리자의 실제 업무 수행)를 추가 질문 없이 시작할 수 있는가 — 가능. 단, 계정 최초 발급/비밀번호 재설정은 의도적으로 운영 문서(11-ops-handoff-runbook.md)로 위임했음을 §1.2에 명확히 안내해, "이 문서 범위 밖"이라는 것 자체가 질문거리가 되지 않도록 처리되어 있음
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거가 명시되어 있는가 — §3(구독자 하드삭제) 전체가 이 목적의 섹션이며, 왜 되돌릴 수 없는지(하드 삭제, 백업은 건별 복구 불가, 14일 후 백업 자체 만료)와 사고 예방 절차(§3.2 5단계)가 구체적으로 명시됨
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — 시크릿/자격증명 원문 없음(재확인), 운영자 전용 인프라 정보(서버 배포/롤백 등)를 사용자 매뉴얼과 섞지 않고 명확히 이 문서 범위에서도 "운영 인수인계 문서로 위임"한다고 경계를 그어 정보 노출/역할 혼입 없음
- 발견된 결함 목록: 없음
- 조치 내용: 없음(추가 수정 불필요, v1을 최종본으로 확정)

## 최종 판정
- [x] PASS (결함 0건 — 2차 기준, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
- [ ] PASS (Low 등급, 1차 검증 결함 0건으로 2차 생략)
- [ ] FAIL

## 비고
- 이 검증은 `11-admin-manual.md` 1건만을 대상으로 하며, 병렬로 작성되는 `11-ops-handoff-runbook.md`/`11-user-manual.md`/`11-api-reference.md`는 별도 호출·별도 검증 로그의 범위다(작업 지시의 "병렬 처리" 절).
- 09단계 산출물의 "10단계에서 이미 검증된 배포/롤백/모니터링 절차는 재검증하지 않는다" 원칙에 따라, 이 문서(관리자 매뉴얼)는 배포/롤백 절차를 다루지 않으며 해당 내용은 전부 `docs/harness/11-ops-handoff-runbook.md`로 위임했다 — 중복 검증/중복 문서화 없음을 확인.
