# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/units/unit-09-test.md`
- 작성 에이전트: `06-unit-tester`
- 버전: v0 (초안)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): `06-unit-tester` (작성자 본인, 자가 재검토)
- 일시: 2026-09-17
- 체크리스트
  - [x] 입력 계약(Input Contract)에 명시된 모든 입력을 실제로 반영했는가 — `unit-09-note.md` §8의 AC1~22 전부를 TC-001~022로 1:1 매핑했다(§4.1). 매핑표를 AC 번호 순서대로 재대조해 누락 없음을 확인했다. 작업 지시가 별도로 요구한 "5단계가 '고쳤다'고 주장한 dispatch() 레벨 체크 vs 전역 CSRF 미들웨어" 재현도 §4.2에 별도 섹션(AC 범위 밖임을 명시)으로 반영했다.
  - [x] 출력 계약(Output Contract)에 명시된 모든 필수 항목이 빠짐없이 존재하는가 — `test-report-template.md`의 9개 섹션(1~9) 전부 채웠다. "해당 없음" 처리한 섹션은 없다(전부 실측 데이터로 채워짐).
  - [x] 상위 단계 산출물과 용어/수치/범위가 모순되지 않는가 — `collectstatic` 218/638 수치(TC-018)를 `unit-09-note.md` §3-3과 대조해 정확히 일치 확인. DEC-028/029/030 참조 번호가 `decisions.md` 실제 항목과 일치함을 재확인. 신규 DEC-031 항목이 `decisions.md`에 실제로 존재하는지 파일을 다시 열어 재확인.
  - [x] 추측으로 채운 항목이 있는가 (있다면 "가정" 섹션에 명시했는가, 혹은 질문으로 전환했는가) — 추측으로 채운 항목을 찾던 중 TC-017의 "실제 결과" 셀에서 문제를 발견했다: 최초 작성 시 Windows 콘솔(cp949)에 출력된 한글이 깨져 보였는데도(`ensure_superuser` 실행 로그가 `?????` 형태로 출력) 필자가 Django 표준 검증기 메시지를 "기억/추정"으로 재구성해 "너무 흔히 사용되는 비밀번호"+"비밀번호가 숫자로만 되어 있습니다"라고 적었다 — 이는 규칙C("실행해보니 에러 없음"만으로 PASS 처리 금지, 기대와 실제를 정확히 대조해야 함)를 위반하는 추측성 기재다.
  - [x] 20년차 실무자 기준으로 봤을 때 구조적/논리적 결함이 없는가 — DEF-001의 심각도를 Low로 낮게 매긴 판단(§6)이 근거 없는 낙관이 아닌지 재검토했다. TC-CSRF-02(노이즈가 예산을 선점하지 않음을 실측)가 그 판단의 핵심 근거이므로, 그 테스트 자체가 방법론적으로 올바른지(같은 IP, 캐시 격리 여부) 재확인 — `cache.clear()`를 각 시나리오 시작 시점에 호출해 이전 TC의 카운터 잔재가 섞이지 않았음을 스크립트 순서로 재확인했다.
  - [x] 오탈자, 형식 오류, 누락된 표/그림 참조가 없는가 — 위 TC-017 문제 외 추가 오탈자 없음.
- 발견된 결함 목록:
  1. TC-017의 "실제 결과" 인용문이 콘솔 인코딩 문제로 정확히 확인하지 못한 채 기억에 의존해 재구성된 근사치였다 — 원문과 정확히 일치하지 않을 위험(실제로 "숫자로만"이 아니라 "전부 숫자로", 어순이 다름).
- 조치 내용: (수정 후 → v1) 별도의 독립 임시 venv(`.venv_recheck_tmp`, 조치 확인 후 즉시 삭제)에서 `PYTHONIOENCODING=utf-8`로 표준출력을 파일로 리다이렉트해 `ensure_superuser`를 재실행하고, 그 결과 파일을 UTF-8로 직접 읽어 원문을 그대로 확인했다: `"너무 흔히 사용되는 비밀번호입니다.; 비밀번호가 전부 숫자로 되어 있습니다."`. TC-017 셀을 이 정확한 원문 인용으로 교체했다. 재확인에 쓴 임시 venv/`db.sqlite3`는 확인 즉시 삭제해(§8 절차와 동일) 저장소에 흔적을 남기지 않았다.

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
> "내가 이 문서를 오늘 처음 받아본 심사자라면?"이라는 관점에서, 1차 검증자가 놓쳤을 법한 리스크·엣지케이스·암묵적 가정을 의심하며 재검토한다.
- 검증자(역할): `06-unit-tester` (독립 심사자 관점으로 역할 전환)
- 일시: 2026-09-17
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가 (재확인) — `unit-09-test.md`를 다시 열어 TC-017 셀이 정정된 원문으로 바뀌어 있음을 확인했다. 임시 재확인용 venv/DB 잔재가 없는지 `git status --porcelain`으로 재확인(§8과 동일 결과, 추가 잔재 없음).
  - [x] 엣지 케이스/예외 상황이 누락되지 않았는가 — 재검토 중 "TC-CSRF-01/02가 IP당 카운터만 확인했는데, 애초에 `is_rate_limited()`가 IP가 빈 문자열일 때 `False`를 반환하는 경로(REMOTE_ADDR 누락 시 무제한 허용)는 AC22까지 어디에도 검증되지 않았다"는 점을 발견했다. 이는 `unit-09-note.md` AC 22개 어디에도 명시되지 않은 항목이지만, 규칙C("명백히 위험한 케이스는 범위를 벗어나도 테스트")에 해당할 수 있어 추가로 확인했다: `RateLimitedLoginView` 앞단에 `XForwardedForMiddleware`(production 전용)가 항상 `REMOTE_ADDR`을 채워 두고, dev/기본 WSGI 환경에서도 Django 테스트 클라이언트·실제 WSGI 서버 모두 `REMOTE_ADDR`을 항상 채운다(Django `WSGIRequest`가 `environ["REMOTE_ADDR"]`을 그대로 노출하며, WSGI 스펙상 이 키는 서버가 필수로 채워야 함) — 즉 "IP가 빈 문자열"은 정상 배포 경로에서 도달 불가능한 방어적 코드(fail-open이 아니라 존재하지 않는 값에 대한 방어)로 판단해 새 결함으로 등록하지 않고, 이 판단 근거만 검증 로그에 남긴다(문서 본문에 별도 TC를 추가하면 오히려 "도달 불가능한 경로를 검증했다"는 과잉 주장이 될 수 있어 본문에는 추가하지 않음).
  - [x] 문서만 보고 다음 단계 에이전트가 추가 질문 없이 작업을 시작할 수 있는가 — §7에 9단계(보안검증)가 이어서 봐야 할 구체적 항목 2건(일반 DoS 관점 재검토, 다른 POST 엔드포인트의 유사 패턴 재확인)을 명시해 다음 단계가 맥락 없이 시작하지 않도록 했다.
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거가 명시되어 있는가 — 이번 6단계는 신규 비가역적 결정을 내리지 않았다. DEF-001 수정(docstring 보강 + 회귀 테스트 추가, 런타임 로직 불변)은 Low·가역적 변경이며 DEC-031에 근거를 남겼다.
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — DEF-001 판정(Critical/High 아님)이 이 세션의 가장 중요한 판단이므로 한 번 더 반대 관점에서 의심했다: "만약 운영자가 CSRF 쿠키/토큰 없이도 로그인 폼을 제출할 수 있는 별도 경로(예: API 클라이언트, 모바일 앱)를 나중에 추가한다면?" — 그 경우 DEC-029/DEC-030의 전제("계정 기준 잠금 없음 + IP 기준만")가 CSRF 예외 뷰에 대해서는 성립하지 않게 되므로, §7에 "9단계가 확인할 항목"으로 이미 일반화해 이월한 문구가 이 시나리오까지 포괄하는지 재확인 — 포괄함(CSRF 예외 뷰 자체가 신설되면 "전역 CsrfViewMiddleware가 항상 앞단에 있다"는 이번 판단의 핵심 전제 자체가 깨지므로 그 시점에 반드시 재검증이 필요하다는 점을 §7 문구가 이미 함의). 현재 코드베이스에는 그런 CSRF 예외 뷰가 REQ-010 범위에 없음을 `config/urls.py`/`core/admin_auth.py` 재확인으로 검증했다.
- 발견된 결함 목록:
  1. 없음(위 "IP 빈 문자열" 항목은 결함이 아니라 도달 불가능한 방어적 코드로 판단 — 결함 목록에 등록하지 않음).
- 조치 내용: 해당 없음(결함 0건, 추가 조치 불필요) — v1을 최종본으로 확정.

## 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
