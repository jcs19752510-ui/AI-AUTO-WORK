# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/units/unit-08-test.md`
- 작성 에이전트: `06-unit-tester`
- 버전: v0 (초안)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): `06-unit-tester` (작성자 본인, 자가 재검토)
- 일시: 2026-09-17
- 체크리스트
  - [x] 입력 계약(Input Contract)에 명시된 모든 입력을 실제로 반영했는가 — `unit-08-note.md` §8의 AC1~19 전부를 TC-001~019로 1:1 매핑했다. 매핑표(§4.1)를 AC 번호 순서대로 재대조한 결과 누락 없음을 확인했다.
  - [x] 출력 계약(Output Contract)에 명시된 모든 필수 항목이 빠짐없이 존재하는가 — `test-report-template.md`의 9개 섹션(1~9) 전부 채웠다. "해당 없음" 처리한 섹션은 없다(전부 실측 데이터로 채워짐).
  - [x] 상위 단계 산출물과 용어/수치/범위가 모순되지 않는가 — `render.yaml` 문자열(TC-017), `collectstatic` 218/638 수치(TC-012)를 각각 `unit-08-note.md`/`feature-WU-07-integration-test.md` IT-04와 대조해 정확히 일치함을 확인. 유료전환 4대 기준 문구(TC-009)는 03 §6.3 원문을 그대로 쓴 `core/monitoring.py`의 `UPGRADE_CRITERIA`와 대조 확인.
  - [x] 추측으로 채운 항목이 있는가 (있다면 "가정" 섹션에 명시했는가, 혹은 질문으로 전환했는가) — 추측으로 채운 항목 없음. 모든 "실제 결과" 셀은 이번 세션에서 직접 실행한 명령/스크립트의 stdout/HTTP 응답을 그대로 옮겼다. AC10의 `avg_response_ms==0.0`처럼 얼핏 이상해 보일 수 있는 값은 §6 비고에 원인(로컬 요청의 낮은 지연시간)을 명시해 추측이 아닌 근거를 남겼다.
  - [x] 20년차 실무자 기준으로 봤을 때 구조적/논리적 결함이 없는가 — 권한 계층(익명/staff-no-perm/access_admin-no-superuser/superuser) 4단계 조합 중 AC가 요구한 2단계(AC7/AC8/AC9)에 더해 놓치기 쉬운 3번째 조합("access_admin은 있으나 superuser 아님")을 TC-026으로 별도 추가했다 — 이 조합이 검증 안 됐다면 "403이 아니라 302+플래시"라는 5단계 주장(§2 편차 4번)이 실제로는 다른 경로(AC8과 동일 경로)에서만 관측됐을 위험이 있었다.
  - [x] 오탈자, 형식 오류, 누락된 표/그림 참조가 없는가 — §5 커버리지 절에서 "9건... 10건 표기"라는 자기모순 문구를 발견해 "10건(TC-020~TC-029)"으로 정정했다(v0→v1).
- 발견된 결함 목록:
  1. §5 커버리지 절 문구 오류("추가 위험/경계 케이스 9건... 실제로는 10개 항목") — 표에 있는 항목 수(TC-020~029, 10개)와 서술이 불일치.
- 조치 내용: (수정 후 → v1) §5의 해당 문장을 "추가 위험/경계 케이스 10건(TC-020~TC-029) 전부 실행."으로 정정. 재확인 결과 TC-020~TC-029가 정확히 10개 행으로 §4.2 표에 존재함을 다시 카운트해 확인.

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
> "내가 이 문서를 오늘 처음 받아본 심사자라면?"이라는 관점에서, 1차 검증자가 놓쳤을 법한 리스크·엣지케이스·암묵적 가정을 의심하며 재검토한다.
- 검증자(역할): `06-unit-tester` (독립 심사자 관점으로 역할 전환)
- 일시: 2026-09-17
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가 (재확인) — `sed`로 §5 문구를 정정한 뒤 파일을 다시 확인해 "9건... 10건 표기" 잔재가 없음을 확인.
  - [x] 엣지 케이스/예외 상황이 누락되지 않았는가 — 재검토 중 "AC10의 `record_request`가 캐시 자체 장애 시에도 안전한가"가 §5 게이트2 주장("예외 미전파")과 직결되는데 함수 단위 테스트(TC-022)만으로는 미들웨어 체인을 통과한 실제 요청 경로에서도 동일하게 안전한지 보장하지 못한다고 판단해, 통합 경로 테스트(TC-023, `Client().get("/healthz")`를 캐시 장애 상태에서 호출)를 추가로 설계·실행해 PASS 확인. 또한 `get_usage_snapshot()`이 요청 0건 상태(막 배포 직후)에서 0으로 나누기 예외를 던지지 않는지(TC-024)도 AC에는 없지만 "빈 DB/빈 캐시"라는 이 프로젝트의 반복 원칙(규칙C, 명백히 위험한 빈 입력)에 따라 추가했다.
  - [x] 문서만 보고 다음 단계 에이전트가 추가 질문 없이 작업을 시작할 수 있는가 — §7 리스크 절에 7단계가 참고할 잔존 이슈(docstring-실제동작 불일치, LocMemCache 다중워커 전제)를 명시했고, §2 제외범위에 10단계 이월 항목의 근거(Windows 로컬 제약, MCP 미연동)를 각각 명시해 "왜 안 했는지"까지 추적 가능하게 했다.
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거가 명시되어 있는가 — 이번 6단계는 신규 비가역적 결정을 내리지 않았다(5단계 DEC-026/DEC-027을 재검증만 함). §6 비고에서 `wagtail_hooks.py` docstring 불일치를 발견했지만 "6단계 테스터가 임의로 코드/주석을 고치지 않았다"는 판단 근거(판단이 필요한 서술 변경이라 규칙F 대상)를 명시적으로 남겼다 — 이 결정 자체도 근거 있는 자체판단으로 기록됨.
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — 권한 경계 4조합 중 3조합을 실측(TC-007/008/009/026)해 최소권한 원칙이 실제 코드에서 깨지지 않음을 확인. 성능 관점에서 미들웨어가 캐시 장애 시에도 응답을 막지 않음(TC-023)을 확인해 "모니터링 부가기능이 실제 서비스를 막으면 안 된다"는 운영 원칙이 실증됨. 이메일 파싱 경계값(TC-027~029)에서 잘못된 형식이 앱 기동 자체를 막지 않음을 확인해 가용성 리스크가 없음을 재확인.
- 발견된 결함 목록:
  1. 없음.
- 조치 내용: 해당 없음(결함 0건, 추가 조치 불필요) — v1을 최종본으로 확정.

## 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
