# 의사결정 로그 (Decision Log)

> 규칙 A(모르면 질문)에 따라 사용자에게 질문하고 답을 받았거나, 규칙 A-3(비가역적 결정)에 따라 에이전트가 근거를 갖고 스스로 결정을 내린 경우 반드시 여기에 append한다. 절대 삭제/수정하지 않고 append-only로 유지한다 (과거 결정의 맥락을 잃지 않기 위함).

| ID | 일시 | 단계 | 질문/이슈 | 결정 내용 | 결정자 (사용자 응답 / 에이전트 자체판단) | 비가역성 | 영향받는 산출물 |
|----|------|------|-----------|-----------|------------------------------------------|----------|------------------|
| DEC-001 | 2026-09-16 | 0 (사전절차) | 프로젝트에 MCP(GitHub/GitLab, Playwright/Chrome, Sentry/Datadog, Slack/Teams 등)를 연동할지 여부 (ORCHESTRATOR.md §4-3 필수 절차) | 연동 안 함. 하네스 전 단계를 MCP 없이 기존 방식(코드/문서/Bash 조사, 대화창 텍스트 질문·승인)으로 진행 | 사용자 응답 | Low | 전 단계 공통 (2·8·10·11·12·13단계에서 MCP 대체 수단을 결과서에 명시) |
| DEC-002 | 2026-09-16 | 0 (사전절차) | 신규 사이트의 목적/유형 | 블로그/콘텐츠 사이트 | 사용자 응답 | Medium | 01-trend-analysis, 02-planning |
| DEC-003 | 2026-09-16 | 0 (사전절차) | 기존 준비 자산(디자인 시안/콘텐츠/도메인) 유무 | 없음 — 전부 새로 기획/디자인 | 사용자 응답 | Low | 02-planning, 04-ux-design |
| DEC-004 | 2026-09-16 | 0 (사전절차, 규칙A-③ 비가역적 결정) | 최초 요청은 "워드프레스 신규 사이트를 Render 서버로 구축"이었으나, Render는 관리형 MySQL을 제공하지 않고 워드프레스는 MySQL/MariaDB 전제로 설계되어 PostgreSQL(Neon)과 직접 호환되지 않음(비공식 어댑터인 PG4WP는 플러그인 호환성 리스크가 커 실무 비권장). 이를 사용자에게 알리고 방향 선택을 요청함 | **워드프레스를 포기**하고, PostgreSQL(Neon) + Python 스택으로 기능적으로 동등한 블로그/콘텐츠 사이트를 새로 구축하기로 결정. 배포는 계속 Render 사용. 구체적 Python 프레임워크/CMS 선택(예: Django+Wagtail 등)은 3단계 설계자가 근거와 함께 제안하고 decisions.md에 추가 기록 | 사용자 응답 | High (프레임워크/DB 엔진 전체 교체 — 되돌리기 매우 어려움) | 01-trend-analysis 이하 전 단계 (특히 02-planning, 03-system-design) |
| DEC-005 | 2026-09-16 | 2 (기획서, 규칙A-③ 비가역적 결정) | 사이트 수익화 모델(무료/광고/유료구독) — 02번 기획서 작성 중 에이전트가 규칙A에 따라 질문 목록으로 제기함(선택지마다 데이터 모델·PG 보안범위·개인정보 고지범위가 실질적으로 달라짐) | **완전 무료** (v1 계획대로 진행). 광고/결제 연동 없음. 02-planning.md에 Out-of-Scope로 표시된 REQ-022~024, WU-12(수익화 작업단위)는 계속 보류 상태로 유지 — 추후 별도 요청 시에만 규칙F에 따라 재작업 | 사용자 응답 | High (수익화 모델 변경은 데이터모델·보안범위·법적고지 전반에 영향) | 02-planning.md, 03-system-design (수익화 관련 설계 범위 제외 근거) |
| DEC-006 | 2026-09-16 | 3 (설계, 규칙A-③ 비가역적 결정) | 01 §3-1에서 제시된 Python CMS 후보(Wagtail vs Django-CMS) 중 최종 프레임워크를 3단계가 확정해야 함(DEC-004에서 위임됨) | **Wagtail(Django 5.2 LTS 기반, Wagtail 7.4 LTS 계열로 버전 고정)을 최종 채택.** 근거: (1) PostgreSQL 네이티브 지원, (2) Render 공식 Django 가이드 + Wagtail 전용 배포 사례 다수(01 §3-1, render.com/docs/deploy-django 2026-09-16 재확인), (3) 공식 릴리스 노트(docs.wagtail.org 2026-09-16 직접 조회) 확인 결과 LTS가 7.0→7.4로 계속 갱신되며 유지보수가 활발함이 재확인됨, (4) Wagtail의 Page 리비전/임시저장/예약발행 내장 기능이 REQ-001과 정확히 일치. Django-CMS는 LTS/릴리스 정보를 이번 조사로도 재확인하지 못해("확인 필요") 불확실한 근거로 비가역적 결정을 내리지 않는다는 원칙에 따라 기각. 최신 8.0(비LTS) 대신 7.4 LTS를 고정한 이유는 1인/소규모 운영(가정 A1)에 맞춰 유지보수 안정성을 유행보다 우선했기 때문. 라이선스 BSD-3-Clause(github.com/wagtail/wagtail LICENSE 직접 확인) 확인 완료, 상용/배포와 충돌 없음 | 에이전트 자체판단 (근거: 03-system-design.md §2.1) | High (프레임워크/CMS 선택 — 되돌리기 매우 어려움) | 03-system-design.md §1~§5, 이후 04~08단계 전체 |
| DEC-007 | 2026-09-16 | 3 (설계, 규칙A-③ 비가역적 결정) | 콘텐츠 데이터의 핵심 스키마 구조(콘텐츠 CRUD/편집워크플로를 어떤 모델 구조로 구현할지)를 확정해야 함 | Wagtail의 **Page 트리 상속 모델**(`BlogPostPage`가 Wagtail `Page`를 상속)을 콘텐츠 핵심 스키마로 채택하고, 임시저장·예약발행(`go_live_at`/`expire_at`)·리비전 이력을 Wagtail 코어가 제공하는 `PageRevision` 메커니즘 그대로 사용(커스텀 재구현 안 함). Category/Tag는 별도 Snippet/M2M(django-taggit 패턴)으로 분리. 근거: DEC-006(Wagtail 채택)에 직접 종속된 구조적 귀결이며, REQ-001을 프레임워크 표준 기능으로 충족해 커스텀 개발 리스크를 최소화하기 위함 | 에이전트 자체판단 (근거: 03-system-design.md §3) | High (핵심 스키마 확정 — 되돌리기 매우 어려움) | 03-system-design.md §3, 05단계(WU-02) 구현 |
| DEC-008 | 2026-09-16 | 3 (설계, 규칙A-③ 비가역적 결정) | REQ-006(이미지/미디어 업로드용 S3 호환 오브젝트 스토리지)의 구체적 벤더를 확정해야 함(01 §8-3에서 3단계로 위임) | **Cloudflare R2를 채택.** 후보 AWS S3, Neon Object Storage(베타)와 공식 가격문서(전부 2026-09-16 직접 조회) 기준으로 비교: R2는 이그레스(다운로드) 비용이 영구 무료이고 무료 티어(저장10GB/Class A 100만회/Class B 1000만회)가 제공되며 "프로덕션 비권장" 문구가 없는 정식 GA 제품인 반면, S3는 이그레스 과금이 있어 무료·비영리 MVP(DEC-005)와 비용구조가 맞지 않고, Neon Object Storage는 베타 단계라 DB·스토리지 장애 지점을 한 벤더에 몰아넣게 되어 장애 격리 원칙에 반함. `django-storages`(BSD-3-Clause, github.com/jschneier/django-storages LICENSE 직접 확인) + boto3의 S3 호환 엔드포인트로 연동. 버킷은 `media-public`(공개)/`backup-private`(비공개, DEC-010과 연계)로 분리 | 에이전트 자체판단 (근거: 03-system-design.md §2.3) | High (오브젝트 스토리지 벤더 확정 — 이미지 URL 스킴이 이 벤더에 종속되어 이후 마이그레이션 비용이 큼) | 03-system-design.md §2.3, §3.2, §5.5, WU-03 |
| DEC-009 | 2026-09-16 | 3 (설계, 규칙A-③ 비가역적 결정) | 인증/인가 모델(운영자·에디터 vs 구독자)을 확정해야 함(02 REQ-010) | **Django 세션 기반 인증 + Wagtail 그룹 기반 페이지 권한**을 운영자/에디터 인증 모델로 채택(OAuth/SSO/JWT 도입 안 함). 구독자(REQ-016)는 계정을 만들지 않고 이메일만 수집하는 무인증 모델로 확정. 어드민 마운트 경로를 기본값(`/admin/`)에서 변경해 자동화 공격 노출을 줄임. 근거: 1인/소규모 운영(가정 A1)에서 외부 IdP 연동은 과설계이며, 회원제(REQ-024)가 Out-of-Scope이므로 구독자용 인증 체계 자체가 불필요 | 에이전트 자체판단 (근거: 03-system-design.md §5.1) | High (인증 방식 확정 — 되돌리기 어려움, 이후 회원제 도입 시 재설계 필요) | 03-system-design.md §5.1, §4, WU-09 |
| DEC-010 | 2026-09-16 | 3 (설계) | Neon 무료 PITR(6시간) 한계를 보완할 백업 메커니즘(REQ-013)을 구체적으로 설계해야 함 | **GitHub Actions 스케줄 워크플로(1일 1회)가 `pg_dump`를 실행해 Cloudflare R2의 `backup-private` 버킷에 업로드**하는 방식으로 확정. Render Cron Jobs는 공식 문서(render.com/docs/free, 2026-09-16 확인) 기준 무료 티어로 배포 가능한 서비스 유형에 포함되지 않아(Web Service/Postgres/Key Value만 무료) 제외. GitHub Actions를 쓰면 앱(Render) 장애와 백업 실행이 서로 독립적으로 동작해 장애 격리가 되고, 별도 유료 서비스 없이 무료로 구현 가능. R2 Object Lifecycle 정책으로 14일 후 자동 만료 | 에이전트 자체판단 (근거: 03-system-design.md §1.1, §3.3, §7.3) | Medium (백업 메커니즘 자체는 나중에 교체 가능하나, 데이터 복구 가능성에 직접 영향을 주는 안전장치이므로 신중히 기록) | 03-system-design.md §1.1/§3.3/§7.3, WU-10 |
| DEC-011 | 2026-09-16 | 3 (설계) | Render 무료 티어 콜드스타트(REQ-011) 대응 방식으로 "킵얼라이브 핑을 도입할지 여부"를 확정해야 함 | **킵얼라이브 핑을 v1에서 도입하지 않기로 결정.** 15분 간격 핑으로 스핀다운을 막으면 월 750 무료 인스턴스시간(공식 확인, render.com/docs/free)의 대부분(약 720시간)을 소진해 향후 추가 무료 서비스 여유가 사라짐. Render가 스핀업 중 자체 로딩 페이지를 표시하는 공식 기능을 1차 방어선으로 삼고, 가끔의 콜드스타트 UX 저하는 무료·비영리·저트래픽 MVP(DEC-005) 단계에서 의도적으로 수용. §6.3에 명시된 4가지 기준 중 하나라도 충족 시 유료 Starter 플랜(스핀다운 없음)으로 전환 | 에이전트 자체판단 (근거: 03-system-design.md §6.3) | Medium (운영 정책 수준 결정이며, 유료 전환으로 비교적 쉽게 뒤집을 수 있으나 비용/UX 트레이드오프가 크므로 기록) | 03-system-design.md §6.3, §8, 운영 Runbook(11단계) |
| DEC-012 | 2026-09-16 | 3 (설계) | 캐싱 전략(Neon 무료 컴퓨트시간 보호 목적)을 확정해야 함 | **v1은 Django LocMemCache 기반 뷰 캐시(5~15분 TTL)만 사용**하고, Redis/Render Key Value(무료 제공되나 별도 서비스) 도입은 v1 범위에서 제외. 근거: 현재 트래픽 규모(월 500UV 목표, 02 §5)에서 분산 캐시 도입 이득 대비 추가 서비스/장애지점 운영 복잡도가 크다고 판단(과설계 방지 원칙). 트래픽이 실제로 늘어 재검토가 필요해지면 그때 도입 | 에이전트 자체판단 (근거: 03-system-design.md §2.5) | Low (설정 수준 결정, 언제든 Redis로 교체 가능) | 03-system-design.md §2.5, §6.1 |

| DEC-013 | 2026-09-16 | 5 (개발, WU-01) | Wagtail/Django 프로젝트 골격을 저장소 어디에 배치할지 — 표준 `wagtail start`가 프로젝트 루트에 생성하는 `templates/`(base.html 등)가 이 하네스가 이미 저장소 루트에서 쓰고 있는 `templates/`(decision-log-template.md 등 공통 산출물 양식)와 이름이 충돌함. 02/03/04번 어디에도 정확한 배치 경로가 명시되지 않았음 | Django/Wagtail 프로젝트 전체(`manage.py`, `config/`(설정 패키지), `home/` 등)를 저장소 루트가 아니라 **`webapp/` 하위 디렉터리**에 배치하기로 결정. 설정 패키지명은 Wagtail 공식 예시와 동일한 `config`를 사용 | 에이전트 자체판단 (근거: 폴더명 충돌 회피가 목적이며, 두 갈래 해석이 결과를 크게 바꾸는 비가역적 결정은 아님) | Low (폴더 이름 변경만으로 되돌릴 수 있음) | webapp/ 이하 전체 구조. 향후 WU-02~WU-11이 이 경로(`webapp/home`, `webapp/config` 등)를 기준으로 앱을 추가함 |
| DEC-014 | 2026-09-16 | 5 (개발, WU-01) | 03-system-design.md §2.1에서 "확인 필요"로 남긴 항목 — Wagtail 7.4가 Django 5.2 LTS만 지원하는지, 5.2/6.x를 함께 지원하는지를 WU-01 착수 시 재확인하라고 명시되어 있었음 | `pip download`로 Wagtail 7.4.3의 실제 PyPI 배포 메타데이터(`Requires-Dist: Django>=5.2`, 상한 버전 명시 없음)를 직접 조회해 확인. Django 5.2 LTS와 호환됨이 실측으로 재확인되어 DEC-006의 "Django 5.2 LTS 고정" 방침을 그대로 유지할 수 있음. 다만 상한이 없어 `pip install`만 하면 최신 Django(6.1.1)가 끌려올 수 있으므로, `requirements.txt`에 `Django==5.2.17`을 명시적으로 고정해 LTS 고정 의도가 실제로 지켜지도록 함 | 에이전트 자체판단 (근거: `pip index versions`/`pip download` 실측 조회, unit-01-note.md 게이트2 체크리스트) | Low (버전 핀은 언제든 조정 가능하며 스키마/아키텍처에 영향 없음) | webapp/requirements.txt, 03-system-design.md §2.1 확인 필요 항목 해소(문서 자체는 별도 갱신하지 않고 이 로그로 추적) |
| DEC-015 | 2026-09-16 | 3 (설계, 규칙F 재작업) | 07단계 통합테스터(`feature-WU-01-integration-test.md`, FAIL 판정)가 WU-01을 실제 배포 형태(WSGI+미들웨어+실제 HTTP 요청)로 조립 검증한 결과 DEF-001(Critical)을 발견함 — production 배포 시 `SECURE_PROXY_SSL_HEADER` 미설정으로 `SECURE_SSL_REDIRECT=True`와 결합해 무한 HTTPS 리다이렉트 루프가 발생, 사이트가 100% 접근 불가 상태가 됨. 근본 원인은 03-system-design.md §5.5가 "Render는 로드밸런서에서 TLS를 종료하고 앱에는 평문 HTTP로 전달한다"는 배포 환경 전제를 명시하지 않았던 것(규칙F-1: 근본 원인 단계까지 소급). 같은 카테고리(리버스프록시 뒤 흔한 누락 설정)도 전수 점검이 필요했음 | 03-system-design.md §5.5를 v1.2로 갱신: ① Render TLS 종료 아키텍처를 배포 환경 전제로 명시(§5.5.1), ② `SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")`를 필수 설정으로 정식 추가하고 무한루프 인과관계 명시(§5.5.2), ③ `USE_X_FORWARDED_HOST`는 점검 결과 확인 완료·조치 불필요로 판정(원본 Host 헤더가 변경 없이 전달됨을 IT-04로 실측 확인), ④ `CSRF_TRUSTED_ORIGINS`는 이미 WU-01 구현에 올바르게 존재하던 것을 정식 요구사항으로 격상, ⑤ `X-Forwarded-For` 기반 클라이언트 실제 IP 처리가 완전히 누락되어 있음을 신규 발견(§5.1 로그인 레이트리밋/§5.3 스팸 IP 마스킹의 실효성을 무력화하는 잠재 결함, Medium)하고 5단계 구현 시 반영할 조치 요구사항을 §5.5.4에 명시. 규칙B 내부검증 2회를 이번 재작업 범위에 대해 처음부터 재수행(verify-log_03-system-design.md "재작업 라운드" 참고) | 에이전트 자체판단 (근거: feature-WU-01-integration-test.md IT-02/IT-03/IT-04 실측 결과 + Render 공식 배포 아키텍처) | Medium (설정 값 수정 자체는 되돌리기 쉬우나, 보안(스푸핑 신뢰 경계)·가용성(무한루프로 인한 전면 장애)에 직접 영향을 주므로 신중히 기록) | 03-system-design.md §5.5/§8/§10, 이후 5단계(WU-01 `config/settings/production.py` 코드 수정), 6단계(`unit-01-test.md` 재검증), 7단계(`feature-WU-01-integration-test.md` 재실행) |

| DEC-016 | 2026-09-16 | 5 (개발, WU-03) | (a) 03-system-design.md §1.2 모듈 경계 표는 "`media_storage`(설정 모듈, 별도 앱은 아님)"이라고 명시했지만, 같은 문서 §3.2(DEC-008)는 `CustomImage`/`CustomRendition`이라는 **모델**을 요구한다 — Django/Wagtail에서 모델은 마이그레이션을 가지려면 반드시 `INSTALLED_APPS`에 등록된 앱에 속해야 하므로 "모델은 있는데 앱은 없음"은 기술적으로 불가능하다. (b) R2 버킷 분리(media-public/backup-private) 설정 시, backup 버킷 전용 자격증명이 아직 발급되지 않은 상태에서 자격증명을 어떻게 채울지 | (a) `custom_images`라는 신규 앱을 만들어 `CustomImage`/`CustomRendition`을 배치했다(`wagtail.images`의 앱 라벨 `wagtailimages`와 이름이 겹치지 않도록 `images`가 아닌 `custom_images`로 명명). §1.2의 "별도 앱은 아니다"라는 서술은 "새 도메인 앱(blog/subscribers/legal/core 같은 수준의 앱)을 만들 필요는 없다"는 취지로 해석했고, 모델을 담을 최소 앱 자체는 Django의 구조적 요구사항이므로 두 서술이 실질적으로 모순되지 않는 단일 해석으로 판단해 질문으로 올리지 않았다(WU-01의 `home` 앱 유지 판단과 동일한 성격의 기술적 필연). (b) `STORAGES["backup"]`는 `R2_BACKUP_BUCKET_NAME`이 설정된 경우에만 활성화되고(비어있으면 해당 별칭 자체가 없음 — production 기동을 막지 않음), 전용 자격증명(`R2_BACKUP_ACCESS_KEY_ID`/`SECRET`)이 없으면 media-public 자격증명을 임시로 재사용한다. 이는 "이번엔 설정만, 실사용은 WU-10"이라는 작업 지시를 문자 그대로 따른 것이며, WU-10 착수 시 버킷 범위가 분리된 전용 R2 API 토큰 발급을 재검토하도록 `unit-03-note.md`에 명시적으로 남겼다 | 에이전트 자체판단 (근거: Django 앱 구조의 기술적 제약 및 작업 지시 "이번엔 설정만" 문구) | Low ((a)는 앱 이름 변경만으로 되돌릴 수 있음. (b)는 자격증명 값만 나중에 교체하면 되는 설정 수준 결정이며, 실제 사용 전이라 데이터 영향 없음) | `webapp/custom_images/`, `webapp/config/settings/production.py`(STORAGES["backup"]), `docs/harness/units/unit-03-note.md` |

| DEC-017 | 2026-09-16 | 5 (개발, WU-04) | 03-system-design.md §4는 `GET /blog/<slug>/`·`/category/<slug>/`·`/tag/<slug>/`라는 평면 경로를 못박았지만, WU-02가 `BlogPostPage.parent_page_types = ["home.HomePage"]`로 페이지 트리를 구성해 Wagtail 기본 트리 URL 규칙을 그대로 따르면 게시물 URL은 `/<slug>/`(HomePage가 사이트 루트라 트리 경로에서 `/home/` 접두어가 제거됨)가 되어 03 §4 계약과 어긋난다 — "이 표가 5단계 개발자가 추가 질문 없이 구현을 시작할 수 있는 최소 계약"이라는 03 §4 서술을 근거로, 계약을 임의로 재해석하지 않고 문자 그대로 지키는 쪽을 택함(두 갈래 해석이 아니라 "명시된 계약을 어떻게 기술적으로 만족시킬 것인가"의 구현 문제로 판단해 질문으로 올리지 않음) | 카테고리/태그/게시물 상세를 Wagtail 페이지 트리 서빙이 아니라 `blog/urls.py`의 일반 Django 뷰로 구현하고, `wagtail_urls` catch-all보다 먼저 매칭되게 `config/urls.py`에 배치. 이로 인해 발생하는 "트리 URL(`/<slug>/`)과 정규 URL(`/blog/<slug>/`) 중복 접근 가능" 문제는 Wagtail 공식 문서가 커스텀 라우팅 페이지에 권장하는 패턴(`get_url_parts()` 오버라이드)과 `serve()`를 정규 URL로의 301 리다이렉트로 오버라이드해 해소함(`serve_preview()`는 `serve()`를 호출하지 않는 별도 경로임을 Wagtail 소스로 직접 확인해, OP-03 어드민 미리보기가 이 리다이렉트에 영향받지 않음을 검증). 아울러 django-taggit(Tag)과 Wagtail(WAGTAIL_ALLOW_UNICODE_SLUGS 기본값 True, Wagtail 소스로 직접 확인)의 기본 slugify가 한글 slug를 그대로 생성하므로, Django 기본 `slug:` 경로 컨버터(ASCII 전용)로는 한글 카테고리/태그/게시물 slug가 라우팅되지 않음을 로컬 실행으로 실제로 재현한 뒤 `str:` 컨버터로 교체함 | 에이전트 자체판단 (근거: unit-04-note.md §2, Wagtail 소스 코드(`wagtail/models/pages.py` get_url_parts/route, `wagtail/models/preview.py` serve_preview) 직접 확인 + 로컬 HTTP 재현) | Medium (URL 스킴 자체는 REQ-003/004 라우트 계약과 직결되어 되돌리면 링크/RSS/캐시가 전부 영향받지만, 구현 방식(오버라이드 코드)만 조정하면 되므로 스키마 수준 비가역성은 아님) | `webapp/blog/models.py`(get_url_parts/serve), `webapp/blog/urls.py`, `webapp/config/urls.py`, unit-04-note.md |
| DEC-018 | 2026-09-16 | 5 (개발, WU-04) | 04-ux-design.md §2/§4는 S-01/S-02 구성요소로 NewsletterSubscribeForm과 JSON-LD(BlogPosting)를 나열하지만, 02-planning.md §9는 뉴스레터 수집(REQ-016)을 WU-07로, JSON-LD를 포함한 REQ-005(SEO)를 WU-05로 각각 별도 배정했다. 오케스트레이터의 이번 WU-04 작업 지시도 "홈/글목록, 글상세, 카테고리별 목록, 태그별 목록, RSS 피드, 반응형 레이아웃"만 범위로 명시하고 뉴스레터/JSON-LD는 언급하지 않았다. 04번 화면 명세(컴포넌트 나열)와 02번 REQ-ID 소유권(작업단위 배정) 중 무엇을 이번 WU 경계의 기준으로 삼을지 두 갈래로 해석될 수 있어 판단이 필요했음 | REQ-ID 소유권(02 §9)을 경계 기준으로 채택 — NewsletterSubscribeForm(REQ-016, `POST /newsletter/subscribe/` 백엔드가 아직 없어 폼을 렌더링해도 제출 시 항상 실패함)과 JSON-LD(REQ-005)는 이번 WU에서 구현하지 않고 WU-07/WU-05로 남긴다. 반면 CookieConsentBanner는 REQ-021(GDPR 강화)이 아니라 REQ-008(쿠키 고지) 배너 "메커니즘" 자체이며 서버 API 의존 없이 순수 `localStorage` + 정적 링크(`/cookies/`)로 동작하므로 S-01의 필수 전역 컴포넌트로 이번 WU에 포함했다 — 링크 대상 `/cookies/`는 WU-06 전까지 일시적으로 404이며, 이는 Footer 법적 링크 3종과 동일한 성격의 정상적인 점진적-완성 상태로 판단(둘 다 하드코딩 경로 `<a href>`이며 `{% url %}` reverse를 쓰지 않음 — 아직 존재하지 않는 URL name을 reverse하면 NoReverseMatch로 전체 렌더링이 깨지기 때문) | 에이전트 자체판단 (근거: unit-04-note.md §2, 02-planning.md §9 작업단위 표) | Low (컴포넌트 포함/제외는 각 WU가 진행되며 자연스럽게 채워지는 가역적 순서 문제이며, 데이터 모델/URL 스킴 변경이 필요 없음) | `webapp/blog/templates/blog/*`, `webapp/config/templates/partials/header.html`/`footer.html`, unit-04-note.md |

| DEC-019 | 2026-09-17 | 5 (개발, WU-05) | 03-system-design.md §1.2는 robots.txt/canonical 같은 사이트 전역 SEO 보조 기능을 REQ-011/012(헬스체크·SiteSettings·콜드스타트) 담당 `core` 앱 소유로 지정했는데, 이번 WU-05는 REQ-005(SEO)만 구현하고 REQ-011/012는 각자의 WU(WU-08/09) 몫이다 — `core` 앱을 아직 아무도 만들지 않은 상태에서 REQ-005 몫만 먼저 채워도 되는지 판단이 필요했음(모델 하나 없이 view/context_processor만 있는 앱을 만드는 것이 "과설계"로 비칠 여지) | DEC-016(WU-03이 `custom_images` 앱을 "이번엔 필요한 것만" 원칙으로 신설한 선례)을 그대로 따라 `core` 앱을 신설하고, robots.txt 뷰/canonical URL 컨텍스트 프로세서/JSON-LD 직렬화 템플릿태그만 이번에 채운다. 헬스체크/SiteSettings/콜드스타트 뷰는 담당 WU가 같은 앱에 이어서 추가하도록 앱 docstring에 명시 | 에이전트 자체판단 (근거: DEC-016 선례, 03 §1.2 모듈 경계 표) | Low (앱 이름/파일 배치만으로 되돌릴 수 있음, 도메인 모델이 없어 마이그레이션 영향 없음) | `webapp/core/`, `webapp/config/settings/base.py`(INSTALLED_APPS) |
| DEC-020 | 2026-09-17 | 5 (개발, WU-05) | 03-system-design.md §4는 JSON-LD `author`의 대체값으로 "SiteSettings.site_title"을 명시했지만, 그 모델은 어떤 WU도 아직 만들지 않았다(§1.2가 REQ-011/012 담당으로 지정, DEC-019 참고) — 이번 WU가 REQ-005만 구현하면서 아직 없는 SiteSettings 모델을 앞서 만드는 것은 범위 확장이라고 판단해 만들지 않기로 했고, 대신 무엇으로 "사이트 대표 이름"을 채울지 결정이 필요했음 | Wagtail `Site.site_name`(footer.html이 이미 쓰는 값) → 값이 비어 있으면 `settings.WAGTAIL_SITE_NAME`(feeds.py가 이미 쓰는 항상-값-있는 전역 설정) 순서의 2단계 대체로 구현. 로컬 검증에서 초기 마이그레이션이 만드는 기본 Wagtail Site의 `site_name`이 실제로 비어 있어 `WAGTAIL_SITE_NAME`까지 폴백되는 것을 직접 확인했다 | 에이전트 자체판단 (근거: unit-05-note.md §3 로컬 검증) | Low (SiteSettings가 나중에 생기면 그 값을 최우선으로 추가하기만 하면 되는 가역적 폴백 순서) | `webapp/blog/models.py`(get_json_ld) |
| DEC-021 | 2026-09-17 | 5 (개발, WU-05) | Wagtail `Page.get_full_url()`/기본 `get_sitemap_urls()`는 등록된 Wagtail `Site` 레코드의 hostname을 기준으로 절대 URL을 만드는데, 로컬 검증 중 이 프로젝트의 초기 마이그레이션(`home/migrations/0002_create_homepage.py`)이 만드는 기본 Site의 hostname이 `"localhost"`로 고정되어 있고 이를 실제 운영 도메인으로 갱신하는 절차가 (WU-01~04 어디에도) 없음을 발견했다 — 이 상태로 배포하면 `sitemap.xml`의 모든 `<loc>`이 실제 도메인과 무관하게 `http://localhost/...`로 나가 REQ-005(SEO)의 핵심 목적을 무력화한다(로컬 검증에서 프로덕션 유사 설정 + `SERVER_NAME=example.com` HTTPS 요청으로 실제 재현). 03 §4는 "wagtail.contrib.sitemaps 표준 활용"만 명시했고 이 Site-hostname 의존성 문제까지는 언급하지 않아, 표준 기본 동작을 그대로 둘지 보정할지 판단이 필요했음 | REQ-005(정확한 sitemap.xml)가 이번 WU의 직접 책임 범위이므로, `wagtail.contrib.sitemaps`라는 "표준 프레임워크"는 그대로 쓰되(설계서 문언 준수), `BlogPostPage`/`HomePage`에 `get_sitemap_urls()`를 오버라이드해 Site-hostname 대신 실제 요청(request) 기반 절대 URL(`core.seo.absolute_page_url`)을 쓰도록 보정했다. WU-04의 `BlogPostPage.serve()`(DEC-017, 트리 URL→정규 URL 301 리다이렉트)도 동일한 Site-hostname 의존 메서드(`get_url()`)를 쓰고 있어 이론상 같은 위험군이지만, 그 코드는 WU-04 소유이고 이번 WU-05 작업 지시(REQ-005/017)에 포함되지 않으므로 손대지 않고 unit-05-note.md에 후속 확인 필요 사항으로만 남겼다(범위 외 변경 금지 원칙) | 에이전트 자체판단 (근거: unit-05-note.md §2/§3 로컬 재현·수정 검증) | Medium (URL 계산 방식 자체는 코드 수정만으로 되돌릴 수 있으나, 방치 시 실제 SEO 기능이 프로덕션에서 무력화되는 실질적 결함이므로 신중히 기록. Site.hostname을 운영 도메인으로 갱신하는 절차 자체는 여전히 미해결 — 후속 WU가 다루지 않으면 다른 Site.hostname 의존 기능도 같은 문제를 겪을 수 있음) | `webapp/core/seo.py`, `webapp/blog/models.py`(get_sitemap_urls/get_json_ld), `webapp/home/models.py`(get_sitemap_urls), unit-05-note.md |

| DEC-022 | 2026-09-17 | 5 (개발, WU-06) | 03-system-design.md §3.2가 개인정보처리방침/이용약관/쿠키고지 3종을 "각각 별도 Page 서브클래스(**또는** 공통 RichTextPage 1개 모델을 재사용)"로 명시해 두 옵션을 모두 허용했다 — 어느 쪽을 택하든 03 §4 라우트 계약(`/privacy-policy/`, `/terms/`, `/cookies/`)과 04-ux-design.md S-05~07 화면 명세를 동일하게 충족할 수 있어(두 옵션 모두 이미 설계서가 명시적으로 승인한 선택지이므로) 규칙A 질문 대상은 아니라고 판단했다 | **공통 재사용 모델(`legal.LegalPage`, RichTextField 1개)을 채택**했다. 근거: (1) 세 페이지 모두 구조가 동일(제목+리치텍스트 본문+최종수정일)해 서브클래스 3개를 만들면 코드 중복만 늘고 실질적 차별화 로직이 없다(과설계 방지 원칙), (2) `parent_page_types=["home.HomePage"]`로 세 인스턴스를 slug=privacy-policy/terms/cookies로 HomePage 직계 자식에 생성하면, HomePage가 사이트 루트라(DEC-017 재확인 로직과 동일) Wagtail 기본 트리 URL이 접두어 없이 자동으로 03 §4 경로와 정확히 일치해, blog 앱처럼 URL 오버라이드가 전혀 필요 없다. 04 §2 S-06의 "목차(TOC) 앵커 링크" 요구는 페이지 종류별로 분기하지 않고 "본문에 h2 제목이 있으면 자동으로 목차 생성"이라는 일반 규칙으로 구현해 단일 모델로도 충족했다(legal/models.py `_add_heading_anchors`) | 에이전트 자체판단 (근거: 03 §3.2가 이미 "또는"으로 양쪽 선택지를 승인, unit-06-note.md §2) | Low (모델을 3개 서브클래스로 분리하고 싶어지면 각 인스턴스를 새 서브클래스로 마이그레이션하는 별도 작업으로 나중에 가능 — 되돌리기 쉬움) | `webapp/legal/models.py`, `webapp/legal/migrations/0001_initial.py`, `webapp/legal/migrations/0002_create_legal_pages.py`, `webapp/legal/templates/legal/legal_page.html` |

| DEC-023 | 2026-09-17 | 5 (개발, WU-07) | 이번 WU-07 작업 지시(오케스트레이터 메시지)는 "구독 취소 토큰 URL을 생성하는 모델/뷰 레벨까지는 구현하라"고 명시했다. 그러나 03-system-design.md §3.2/§5.3/§5.6(PASS, v1.2)은 "v1은 자동 구독취소 링크가 없으므로... 자동 구독취소 링크를 만들 수 없다", "v1의 실제 삭제 경로는 운영자의 수동 하드 삭제뿐이다"라고 반복적으로, 명시적으로 못박았고, 02-planning.md §9/가정 A9도 WU-07 범위를 "이메일 수집까지"로만 한정한다. 게다가 WU-06이 이미 배포한 개인정보처리방침 본문(`legal/migrations/0002_create_legal_pages.py`)이 "본 사이트는 현재 자동 구독취소 기능을 제공하지 않습니다"라고 이용자에게 명시적으로 고지한 상태다 — 토큰/URL을 새로 만들면 PASS된 상위 설계서 및 이미 게시된 법적 고지 문구와 정면으로 모순된다 | 오케스트레이터 지시보다 PASS 상태인 03/02 설계서와 이미 게시된 WU-06 법적 문구를 우선해 **구독 취소 토큰/URL을 이번 WU에서 구현하지 않는다.** `NewsletterSubscriber.status`(active/unsubscribed) 필드는 03 §3.2가 이미 "선제적(forward-compatible) 스키마"로 지시한 대로 그대로 준비해 두되, 토큰 생성/검증 로직과 라우트는 만들지 않았다. 실제 자동 구독취소 기능은 발송 인프라가 갖춰지는 별도 WU에서 03 설계서 갱신(규칙F)과 함께 착수해야 한다 — 지금 임의로 토큰 스킴을 설계하면 발송 인프라 WU가 다시 뒤집어야 할 가능성이 높아 과설계이기도 하다 | 에이전트 자체판단 (근거: 03 §3.2/§5.3/§5.6 명시적 반복 서술, 02 §9/가정 A9, WU-06 게시된 개인정보처리방침 본문) | Low (토큰 필드/뷰는 나중에 추가만 하면 되는 순수 추가 기능이라 지금 안 만든다고 되돌릴 것이 없음 — 다만 오케스트레이터 지시와 다른 방향으로 자체판단했으므로 확인을 요청한다, unit-07-note.md §1 "질문 목록" 참고) | `webapp/subscribers/models.py`, `webapp/subscribers/views.py`, unit-07-note.md |

| DEC-024 | 2026-09-17 | 5 (개발, WU-07) | 03-system-design.md §3.2는 `NewsletterSubscriber.consent_version`에 "구독 동의 시점의 개인정보처리방침 버전 스냅샷"을 저장하라고 명시했지만, DEC-022로 확정된 `legal.LegalPage`는 별도 버전 필드를 두지 않는 단일 RichTextField 모델이라 "버전"을 어디서 가져올지 설계서에 구체적 소스가 없었다. 또한 §5.3은 "IP 기준 레이트리밋"을 요구하지만 구체적 구현 수단(전용 패키지 vs 기존 인프라 재사용)을 지정하지 않았다 | (1) `subscribers/constants.py`에 `PRIVACY_POLICY_VERSION` 문자열 상수(WU-06 개인정보처리방침 최초 게시일)를 두고 이를 `consent_version`에 스냅샷한다. `subscribers`가 `legal` 앱을 import하지 않는 이유는 03 §1.2 "경계 원칙"(개인정보 취급 앱과 공개 콘텐츠 앱 격리)을 그대로 지키기 위함이며, 이는 개인정보처리방침 본문이 바뀔 때마다 이 상수를 수동으로 갱신해야 하는 운영 절차를 수반한다(unit-07-note.md에 명시). (2) 레이트리밋은 새 패키지(django-ratelimit 등, 03 §5.1이 로그인 레이트리밋용으로 "라이선스 확인 후 5단계에서 도입"이라 언급한 것과는 별개 용도)를 추가하지 않고, DEC-012가 이미 채택한 `LocMemCache`를 그대로 재사용해 IP당 10분 5회로 제한했다 — 운영 규모 대비 과설계 방지(DEC-012와 동일 원칙). LocMemCache는 프로세스 로컬이라 Gunicorn 워커가 여러 개면 워커별로 카운터가 분리되는 한계가 있음을 그대로 인지하고 기록한다(v1 단일 인스턴스·저트래픽 전제에서는 허용 가능한 트레이드오프) | 에이전트 자체판단 (근거: 03 §3.2/§5.3에 구체적 소스/수단이 없어 합리적 기본값 선택, DEC-012 선례) | Low (둘 다 나중에 값/구현을 교체해도 스키마·API 계약에 영향 없음) | `webapp/subscribers/constants.py`, `webapp/subscribers/views.py`, unit-07-note.md |

| DEC-025 | 2026-09-17 | 5 (개발, WU-07) | 뉴스레터 폼을 `@cache_page`가 걸린 공개 페이지(홈/카테고리/태그/게시물 상세, WU-04/05/06 소유)에 직접 렌더링하려 했으나, 로컬 검증(`Client(enforce_csrf_checks=True)`)에서 실제로 재현한 치명적 결함을 발견했다: 캐시 HIT은 뷰를 재실행하지 않아 `{% csrf_token %}`의 `get_token()`이 호출되지 않고, 그 결과 (a) 캐시를 채운 첫 방문자 이후의 모든 방문자는 CSRF 쿠키 자체를 받지 못하거나 (b) 쿠키를 받아도 캐시에 박제된 첫 방문자의 토큰과 일치하지 않아, 뉴스레터를 구독하려는 시도가 캐시 TTL(10분) 동안 사실상 100% `403 CSRF` 오류로 실패한다(REQ-016 핵심 기능 자체가 프로덕션에서 동작하지 않는 결함). 03 설계서는 이 캐시/CSRF 상호작용을 언급하지 않았다 | 캐시된 페이지에는 정적인 자리표시자(`subscribers/_newsletter_slot.html`, CSRF와 무관한 `{% url %}` 경로만 포함)만 남기고, 실제 폼은 **캐시하지 않는** 두 개의 새 엔드포인트로 분리했다: `GET /newsletter/form/`(JS가 fetch로 채우는 조각, 매 요청마다 새로 렌더링되어 항상 방문자 본인의 쿠키와 일치하는 토큰을 만든다)과 `GET /newsletter/`(무-JS 폴백 전용 페이지, `noscript` 링크의 목적지). 이 방식은 (1) 홈/카테고리/태그/게시물 상세의 기존 `@cache_page` 설정(DEC-012, WU-04/05/06 소유 코드)을 전혀 건드리지 않고, (2) 04 §0 "모든 화면은 JS 없이도 동작해야 한다" 원칙을 그대로 지키며, (3) 로컬 재현 시나리오(서로 다른 두 방문자가 각자 프래그먼트를 받아 각자 구독 성공)로 수정 결과를 검증했다(unit-07-note.md §3, `subscribers/tests.py::NewsletterCachedPageCsrfTests`가 회귀 테스트로 고정). 대안으로 검토했으나 기각한 것: ① 해당 페이지들의 `@cache_page`를 아예 제거 — 다른 WU가 확정한 캐시 전략을 통째로 무력화해 과도함, ② `@csrf_exempt` — 03 §5.2가 "`/newsletter/subscribe/` 포함 모든 POST 폼에 CSRF 토큰 필수"라고 명시적으로 요구해 채택 불가 | 에이전트 자체판단 (근거: `Client(enforce_csrf_checks=True)`로 직접 재현·검증, unit-07-note.md §3) | Medium (구현 방식 자체는 되돌리기 쉬우나, 방치 시 REQ-016 핵심 기능이 프로덕션에서 완전히 동작하지 않는 실질적 결함이었으므로 신중히 기록) | `webapp/subscribers/views.py`, `webapp/subscribers/urls.py`, `webapp/subscribers/templates/subscribers/_newsletter_slot.html`, `webapp/subscribers/templates/subscribers/newsletter_page.html`, `webapp/config/static/js/newsletter.js`, unit-07-note.md |

| DEC-026 | 2026-09-17 | 5 (개발, WU-08) | `feature-WU-07-integration-test.md` IT-20이 `render.yaml`에 `--workers`/`WEB_CONCURRENCY` 지정이 없어 gunicorn 기본값(워커 1개)으로 동작 중임을 확인하고 "현재는 Low 리스크이나 향후 워커를 늘리면 LocMemCache 기반 레이트리밋(DEC-024)이 워커별로 갈라질 수 있다"고 이월했다. 이번 WU-08 작업 지시(오케스트레이터 메시지)도 "의도된 것인지 확인하고, 명시적으로 문서화하거나 필요시 render.yaml에 명시적으로 고정하라(가역적 결정이므로 자체판단 가능)"고 명시적으로 위임했다 | `startCommand`에 `--workers 1`을 **명시적으로 고정**했다(암묵적 기본값에서 명시적 설정으로 전환). 근거: 이 값은 이미 03 §6.2("v1은 Render 웹 서비스 1개(단일 인스턴스)로 설계")가 전제하는 아키텍처의 자연스러운 연장이고, DEC-024(뉴스레터 레이트리밋)뿐 아니라 이번 WU-08이 새로 추가하는 사용량 카운터(`core/monitoring.py`)도 동일하게 "단일 프로세스" 전제 위에서만 정확하다 — 두 기능 모두를 위해 이 전제를 우연한 기본값이 아니라 의도된 설정으로 못박아야 향후 Render 플랫폼이 기본 동작을 바꾸거나 누군가 무심코 `WEB_CONCURRENCY`를 설정해도 깨지지 않는다. 워커를 늘려야 할 필요가 생기면(트래픽 증가 등) 그 시점에 Redis 등 공유 저장소로 먼저 전환하는 것을 선행 조건으로 삼는다 | 에이전트 자체판단 (근거: `feature-WU-07-integration-test.md` IT-20, 03 §6.2, 오케스트레이터의 명시적 위임) | Low (설정값 변경만으로 언제든 되돌리거나 늘릴 수 있음 — 단, 늘리기 전에 공유 저장소 전환이 선행되어야 한다는 조건이 있음) | `webapp/render.yaml`, `webapp/core/monitoring.py`, unit-08-note.md |
| DEC-027 | 2026-09-17 | 5 (개발, WU-08) | 03-system-design.md §6.3은 "별도의 자체 사용량 추적 배치 잡은 두지 않는다(그 잡 자체가 시간을 소모하고 과설계이기 때문)"라고 명시적으로 결정했다. 그런데 이번 WU-08 작업 지시(오케스트레이터 메시지)는 REQ-012의 "4가지 유료전환 기준을 실제로 추적할 수 있는 기술적 장치"로 "간단한 관리자 대시보드... 최근 요청 수/응답시간 등 기본 지표를 노출"을 요구했다 — 문언만 보면 PASS 상태인 03 설계서의 "배치 잡 없음" 결정과 상충하는 것처럼 보일 수 있어, 이 둘을 어떻게 정합시킬지(설계서를 규칙F로 다시 열어야 하는지, 아니면 다른 종류의 구현이라 상충이 아닌지) 판단이 필요했다 | **03 §6.3이 거부한 것은 "배치 잡"(주기적으로 스스로 깨어나 Render/Neon 외부 API를 폴링하는 별도 스케줄 작업)이고, 이번에 만든 것은 이미 발생하는 요청 처리 경로에 얹은 인메모리 카운터(미들웨어)다 — 종류가 다르므로 상충이 아니라고 해석**하고 규칙F(설계서 재작업)를 트리거하지 않았다. 대신 "측정 가능한 것/불가능한 것"을 코드와 대시보드 문구에 정직하게 구분했다: ①(월 500UV 추세)만 요청량을 참고 지표로 제공하고, ②(Render 인스턴스시간)·③(Neon 컴퓨트/저장)은 앱 프로세스 내부에서 원천적으로 관측 불가능한 플랫폼 지표임을 명시해 03 §7.2/§7.4가 이미 설계한 Render 자동 이메일+운영자 수동 확인 체계로 안내한다(과장된 "전량 자동 모니터링"을 참칭하지 않음) | 에이전트 자체판단 (근거: 03 §6.3 원문 해석, `core/monitoring.py` docstring에 동일 논거 기록) | Low (구현을 걷어내도 배치 잡을 새로 만든 상태가 아니므로 03 §6.3 결정 자체를 되돌릴 필요가 없음 — 순수 추가 기능) | `webapp/core/monitoring.py`, `webapp/core/middleware.py`, `webapp/core/templates/core/usage_dashboard.html`, unit-08-note.md |
| DEC-028 | 2026-09-17 | 5 (개발, WU-09) | Wagtail 그룹/권한 범위를 확정하는 과정에서, `blog.Category`(REQ-002, WU-02가 `@register_snippet`으로 등록)의 기본 권한 정책이 Django 표준 모델 권한(`blog.add_category`/`change_category`/`delete_category`, Wagtail 소스 `wagtail/snippets/views/snippets.py`의 `permission_policy` 프로퍼티로 직접 확인)임을 발견했다. 그런데 이 권한을 Editors/Moderators 그룹에 부여하는 마이그레이션이 WU-02 어디에도 없었다 — Wagtail 코어(`wagtailcore 0002_initial_data`)가 자동으로 주는 것은 **페이지** 권한뿐이고, 스니펫 모델 권한은 그 스니펫을 도입하는 쪽이 직접 부여해야 하는 Wagtail 표준 패턴이다. 이 상태로는 슈퍼유저가 아닌 Editors/Moderators 사용자가 카테고리를 CRUD할 수 없다 — REQ-010(권한관리) 범위에서 반드시 고쳐야 하는 실질적 결함인지, 아니면 "카테고리는 애초에 슈퍼유저만 관리하는 것이 의도"였는지 설계서에 명시가 없어 판단이 필요했다 | 03-system-design.md §5.1이 "Wagtail 그룹 기반 **페이지** 권한"이라고만 썼을 뿐 스니펫을 명시적으로 배제하지 않았고, REQ-002(카테고리 분류체계) 자체가 "Should는 아니고 Must" 요구사항이며 02-planning.md 어디에도 "카테고리는 슈퍼유저 전용"이라는 제약이 없다 — 즉 이 결함을 그대로 두면 1인 이상으로 운영자가 늘어나는 순간(가정 A1이 "현재는 1인"이지 "영구히 1인"은 아님) REQ-002 자체가 슈퍼유저 병목이 된다. **`core/migrations/0001_setup_editor_permissions.py`를 신설해 Editors/Moderators 그룹에 카테고리 add/change/delete 권한을 명시적으로 부여**했다(Wagtail 자체의 `wagtailimages/migrations/0002_initial_data.py`와 동일한 `get_or_create` 패턴을 그대로 참고 — post_migrate 시그널이 Permission 행을 만들기 전에 이 데이터 마이그레이션이 실행되므로 직접 만들어야 함). 반면 이미지(`custom_images.CustomImage`) 권한은 손대지 않았다 — Wagtail이 `WAGTAILIMAGES_IMAGE_MODEL`을 스왑해도 권한 코드네임이 항상 원본 `wagtailimages.Image`에 고정됨을 `wagtail/images/permissions.py`(`CollectionOwnershipPermissionPolicy(get_image_model(), auth_model=Image, ...)`) 소스로 직접 확인했고, 기존 Wagtail 마이그레이션이 이미 Editors/Moderators에 올바르게 권한을 부여해 두었음을 로컬 테스트(`core/tests.py` `CategoryEditorPermissionTests`)로 재확인했다 | 에이전트 자체판단 (근거: Wagtail 소스 코드 직접 확인 + 로컬 Django 테스트로 실측, unit-09-note.md §2) | Low (권한 부여/회수는 마이그레이션 되돌리기로 언제든 가역적이며, 데이터 모델 변경이 아님) | `webapp/core/migrations/0001_setup_editor_permissions.py`, `webapp/core/tests.py`, `webapp/ADMIN_ACCESS_GUIDE.md` |
| DEC-029 | 2026-09-17 | 5 (개발, WU-09) | 03-system-design.md §5.1/§2.6은 로그인 무차별대입 방어를 "IP/계정 기준 레이트리밋"이라고만 쓰고 구체적 구현 수단을 django-axes/django-ratelimit 후보로 제시하며 "라이선스 확인 후 5단계에서 도입"이라고 위임했다(§2.6 "확인 필요"). (1) 새 패키지를 실제로 도입할지, DEC-024(뉴스레터 레이트리밋)처럼 LocMemCache를 재사용할지, (2) "IP/계정 기준"이라는 문구가 두 기준을 **모두** 구현하라는 뜻인지 판단이 필요했다 — 계정 기준 잠금까지 구현하면 공격자가 알려진 운영자 계정명(관리자 이메일 등은 사이트 어딘가에 노출될 수 있음)으로 의도적으로 여러 번 실패를 유도해 정당한 1인 운영자 본인의 계정을 잠그는 자기서비스거부(self-DoS) 경로가 새로 생긴다 | (1) 이번 작업 지시(오케스트레이터 메시지)가 "WU-07(DEC-024)이 이미 LocMemCache 기반 레이트리밋 패턴을 사용했으니 일관성 있게 재사용 검토, 새 패키지 도입은 근거 필요"라고 명시적으로 위임한 것을 그대로 따라 **새 패키지를 도입하지 않고 LocMemCache 기반 카운터(`core/admin_auth.py`)를 재사용**했다 — 03 §2.6의 "확인 필요"(라이선스 재확인) 자체를 회피하는 효과도 있다. (2) **IP 기준으로만 제한하고 계정 기준 잠금은 두지 않는다** — 1인/소규모 운영(가정 A1)에서 계정 잠금은 대체 관리자가 없어 자기서비스거부에 대한 복구 수단이 마땅치 않고, DEC-009가 이미 2FA조차 "기록만 하고 v1 구현범위 제외"로 결정한 것과 같은 리스크 감수 수준(1인 운영 규모에서 IP 기준만으로도 실질적 방어 효과가 충분하다고 판단)이다. 임계값은 15분당 IP 10회로 정했다(뉴스레터 스팸 방지 5회/10분보다 여유를 둠 — 정당한 운영자 본인의 오타 재시도를 과도하게 막지 않기 위함) | 에이전트 자체판단 (근거: 오케스트레이터의 명시적 위임 문구, DEC-009/DEC-024 선례, 1인 운영 가정 A1) | Low (임계값/구현 방식은 코드 수정만으로 언제든 조정 가능, 계정 기준 잠금을 나중에 추가하고 싶으면 별도 기능으로 얹을 수 있음) | `webapp/core/admin_auth.py`, `webapp/config/urls.py`, `webapp/ADMIN_ACCESS_GUIDE.md` |
| DEC-030 | 2026-09-17 | 5 (개발, WU-09) | 이번 작업 지시(오케스트레이터 메시지)가 "Render 환경에서 `manage.py createsuperuser`를 어떻게 안전하게 실행하는지" 문서화를 요구했다. Render 공식 문서(render.com/docs/free "Other limitations", 2026-09-17 직접 curl 조회)를 확인한 결과 **Free 웹 서비스는 Shell 접속(SSH/대시보드 Shell)과 one-off Job 실행을 지원하지 않는다**는 사실을 발견했다 — DEC-011이 v1을 Render 무료 티어로 유지하기로 이미 확정했으므로, 배포된 인스턴스에 대화형으로 접속해 `createsuperuser`를 실행할 방법이 애초에 없다. 단순 "가이드 문서만 작성"으로는 실제로 실행 불가능한 절차를 안내하는 결과가 되어, 문서화만으로 이 작업을 끝낼 수 없었다(허위 운영 절차를 남기는 것은 더 위험하다고 판단) | **`build.sh`(모든 요금제에서 배포마다 실행되는 빌드 단계 — `migrate --noinput`이 이미 이 방식이 free 티어에서도 동작함을 증명)에 `manage.py ensure_superuser`(신규 커스텀 관리 명령, `core/management/commands/`)를 추가**해 환경변수(`DJANGO_SUPERUSER_USERNAME/EMAIL/PASSWORD`, `render.yaml`에 `sync: false`로 추가) 기반으로 최초 계정을 멱등하게(이미 슈퍼유저가 있으면 아무것도 하지 않음) 부트스트랩하도록 구현했다. 03 §5.1의 비밀번호 정책(`AUTH_PASSWORD_VALIDATORS`)을 `validate_password()`로 재사용해 약한 값이 그대로 슈퍼유저 비밀번호가 되는 것을 막았다. 최초 계정 생성 후에는 운영자가 세 환경변수를 대시보드에서 지우도록 `ADMIN_ACCESS_GUIDE.md`에 권고했다(평문 비밀번호를 환경변수에 계속 남겨두지 않기 위함, 멱등 커맨드라 지워도 안전) | 에이전트 자체판단 (근거: render.com/docs/free 공식 문서 2026-09-17 직접 조회, DEC-011 free 티어 유지 결정) | Low (부트스트랩 커맨드는 멱등하고 언제든 build.sh에서 제거 가능하며, 기존 슈퍼유저 계정에 영향을 주지 않음) | `webapp/build.sh`, `webapp/core/management/commands/ensure_superuser.py`, `webapp/render.yaml`, `webapp/ADMIN_ACCESS_GUIDE.md`, `webapp/core/tests.py` |
| DEC-031 | 2026-09-17 | 6 (단위테스트, WU-09) | 직전(중단된) 세션이 남긴 의심 — `unit-09-note.md` §3이 "`post()`가 아니라 `dispatch()`에서 확인해 CSRF 통과 여부와 무관하게 카운트하도록 고쳤다"고 주장했는데, 이 프로젝트는 `django.middleware.csrf.CsrfViewMiddleware`를 전역 MIDDLEWARE에 항상 포함하고 있고, 이 미들웨어는 `process_view()` 단계에서 뷰(따라서 `RateLimitedLoginView.dispatch()` 포함)가 호출되기도 전에 403으로 요청을 끝낼 수 있다 — 그렇다면 "dispatch() 레벨로 고쳤다"는 수정이 이 전역 경로에는 실제로 도달하지 못해 무력화되는 것 아닌지 직접 재현 확인이 필요했다 | `Client(enforce_csrf_checks=True)`로 실측 재현한 결과 의심은 **부분적으로 사실**로 확인됐다: CSRF 토큰이 없거나 틀린 POST는 전부 403이며, 15회를 보내도 레이트리밋 카운터(`cache.get`)가 단 한 번도 증가하지 않았고 429도 나오지 않았다(반면 `RequestFactory` + `XForwardedForMiddleware`만 감싼 경로 — 전역 CsrfViewMiddleware가 없는 경로 — 에서는 10회 모두 403이면서도 11번째에 정확히 429가 나와, `post()`→`dispatch()` 이동 자체는 부모 클래스 데코레이터 문제를 실제로 고쳤음도 함께 확인됐다). 그러나 이어서 실측한 결과 **이것이 REQ-010의 무차별대입 방어 목적을 무력화하는 결함은 아니라고 판단**했다 — 근거: (1) CSRF가 실패하는 요청은 사용자명/비밀번호를 전혀 검사하지 않고 항상 동일하게 403이므로 공격자가 자격증명을 단 하나도 시험해볼 수 없다(정보 이득 0). (2) 자격증명을 실제로 시험할 수 있는 모든 요청은 반드시 유효한 CSRF 토큰을 전제로 하며, 그런 요청은 예외 없이 카운터를 통과한다(동일 IP에 CSRF-무효 트래픽을 15회 흘려보낸 뒤 유효한 CSRF로 전환해도 정확히 새 10회 예산을 그대로 받음을 실측 확인 — CSRF-무효 노이즈가 예산을 선점/소모하지 않음). 즉 Critical/High로 보고할 기능적 보안 결함은 아니라고 결론 내렸다. 다만 `core/admin_auth.py` 모듈 docstring이 "CSRF 통과 여부와 무관하게 카운트해야 한다"는 의도만 서술하고 이 경계를 언급하지 않아 향후 유지보수자를 오도할 수 있는 **문서 정확성 결함(Low)**으로 분류해 6단계가 직접 수정했고 (전역 미들웨어 경계를 명시하는 문단 추가), 이 경계를 고정하는 회귀 테스트 2건(`test_csrf_rejected_requests_are_not_counted_but_cannot_test_credentials`, `test_csrf_rejected_noise_does_not_consume_budget_for_later_valid_attempts`)을 `core/tests.py`에 추가했다(`unit-09-test.md` TC-CSRF-01/02) | 에이전트 자체판단 (근거: `Client(enforce_csrf_checks=True)` + 캐시 카운터 직접 조회로 실측, 대조군으로 `RequestFactory`+`XForwardedForMiddleware` 경로도 함께 실측해 두 경로의 차이 원인을 특정) | Low (docstring/주석 정정과 테스트 2건 추가뿐이며 런타임 동작은 변경하지 않음 — 코드 로직은 그대로 두고 문서·테스트만 보강) | `webapp/core/admin_auth.py`, `webapp/core/tests.py`, `docs/harness/units/unit-09-test.md` |

| DEC-032 | 2026-09-17 | 5 (개발, WU-10) | DEC-010이 "GitHub Actions 스케줄 워크플로가 `pg_dump`를 실행해 R2 `backup-private`에 업로드"라는 방향만 정했을 뿐, (1) `pg_dump` 출력 포맷/플래그, (2) 백업 전용 DB 자격증명을 앱 런타임 `DATABASE_URL`과 어떻게 분리할지(03 §5.4는 "권장"이라고만 썼고 강제 여부/구체 방법은 5단계에 위임), (3) 워크플로 트리거를 스케줄만 둘지 수동 트리거도 열지를 확정해야 했다 | (1) **plain SQL 덤프 + gzip 압축**(`pg_dump --no-owner --no-privileges --clean --if-exists --verbose`)을 채택했다 — 1인 운영(가정 A1)에서는 `pg_restore`/커스텀 포맷(`-Fc`)보다 `psql`로 바로 복구 가능한 plain SQL이 이해하기 쉽고, `--no-owner --no-privileges`로 백업 계정과 복구 대상 계정 권한이 달라도 복구가 중단되지 않게 했으며, `--clean --if-exists`로 기존 스키마가 남은 DB에도 안전하게 재적용 가능하게 했다(과설계 방지 원칙). (2) 백업 전용 시크릿 이름을 앱의 `DATABASE_URL`과 다른 **`NEON_BACKUP_DATABASE_URL`**로 명시적으로 분리하고, **워크플로가 이 값을 앱 `DATABASE_URL`로 자동 대체(fallback)하지 않도록** 구현했다 — DEC-016(b)는 WU-03 시점에 R2 백업 자격증명을 media-public 자격증명으로 임시 대체하는 것을 허용했지만, 이번 WU-10은 03 §5.4가 명시한 "GitHub Actions라는 별도 신뢰 경계에 프로덕션 전체 쓰기 권한을 그대로 넘기지 않기 위함"이라는 문구를 DB 자격증명에는 문자 그대로 엄격히 적용했다(DB 쓰기 권한 노출이 R2 media 키 노출보다 파급력이 크다고 판단). (3) `schedule`(1일 1회, DEC-010) 외에 `workflow_dispatch`도 함께 열었다 — 03 §3.3-3이 이미 "destructive 마이그레이션 배포 직전 수동 pg_dump 백업"을 요구하고 있어, 별도 스크립트를 새로 만들지 않고 이 워크플로를 수동 트리거하는 것으로 그 요구를 그대로 충족시켰다(범위 확장이 아니라 기존 요구사항의 구현 경로 선택) | 에이전트 자체판단 (근거: 03 §3.3-3/§5.4 원문, 1인 운영 가정 A1, DEC-016(b) 선례와의 의도적 차별화) | Low (플래그/시크릿 이름/트리거 종류는 워크플로 YAML 수정만으로 언제든 바꿀 수 있고, 기존 데이터/스키마에 영향 없음) | `.github/workflows/neon-db-backup.yml`, `webapp/BACKUP_RESTORE_GUIDE.md` |
| DEC-033 | 2026-09-17 | 5 (개발, WU-10) | 03 §7.5는 "`backup-private` 버킷에 R2 Object Lifecycle 규칙을 적용해 14일 후 자동 삭제한다"고 정책만 명시했을 뿐, 이 규칙을 Cloudflare 대시보드에서 수동으로 1회 설정할지, 코드로 자동화할지는 정하지 않았다 | Cloudflare 공식 문서(developers.cloudflare.com/r2/buckets/object-lifecycles/, 2026-09-17 직접 조회)로 R2가 Dashboard/Wrangler CLI뿐 아니라 **S3 호환 API(`PutBucketLifecycleConfiguration`)로도 Lifecycle 규칙 설정을 지원함을 확인**했다. 이미 업로드에 쓰는 것과 동일한 R2 백업 자격증명(`aws s3api`)을 재사용해, **워크플로가 매 실행마다 `db-backups/` 프리픽스에 14일 만료 규칙을 멱등하게 재적용**하도록 자동화했다 — 근거: (1) 1인 운영(가정 A1)에서 "버킷 생성 시 대시보드에서 수동으로 Lifecycle 설정을 잊지 않기"에 의존하는 것은 실수 가능성이 있는 반면, 코드로 자동화하면 버킷이 재생성되거나 설정이 실수로 지워져도 다음 실행에서 저절로 복구된다(자기치유). (2) 새로운 인증 체계나 CLI(wrangler+npm 등)를 추가로 도입하지 않고 이미 쓰는 `aws` CLI+기존 R2 백업 자격증명만 재사용해 과설계를 피했다 | 에이전트 자체판단 (근거: Cloudflare 공식 문서 2026-09-17 직접 조회로 S3 API 지원 확인, 1인 운영 가정 A1, 과설계 방지 원칙) | Low (Lifecycle 규칙은 매 실행마다 같은 내용으로 재적용되는 멱등 설정값이라, 워크플로에서 이 스텝을 제거해도 되돌릴 것이 없음 — 단, 이 스텝을 제거하면 버킷을 처음 만들 때 운영자가 수동으로 Lifecycle을 설정해야 함) | `.github/workflows/neon-db-backup.yml`, `webapp/BACKUP_RESTORE_GUIDE.md` |
| DEC-034 | 2026-09-17 | 6 (단위테스트, WU-10) | `unit-10-test.md` TC-005d가 `.github/workflows/neon-db-backup.yml`의 "Verify required secrets are present" 스텝(`[ -z "$value" ]` 기반 판정)이 공백 문자로만 구성된 시크릿 값을 "설정됨"으로 오인해 통과시키는 경계값 결함(DEF-001, Low)을 발견했다 — AC5(a/b/c)가 요구한 범위 밖이지만 규칙C(위험 케이스는 범위 밖이어도 테스트)에 따라 6단계가 자체 실행했다. 이 결함을 5단계로 되돌려 즉시 재작업을 요구할지, 아니면 낮은 파급력을 근거로 이관만 할지 결정이 필요했다 | **5단계로 되돌리지 않고 관찰/이관으로 처리**했다 — 근거: (1) 이 케이스가 1번 스텝을 통과해도 3번 스텝(`pg_dump ... "$NEON_BACKUP_DATABASE_URL"`)이 공백 문자열을 유효하지 않은 연결 문자열로 받아 `set -euo pipefail` 하에 결국 non-zero로 실패하므로 조용한 성공(silent success)으로 이어지지 않는다(데이터 유실/보안 노출 없음). (2) 실제 운영자가 GitHub Secret 값에 공백 문자만 입력할 확률은 매우 낮다(전형적 값은 URL/키 문자열). (3) 규칙F의 재작업 루프는 Critical/High 결함에 우선 적용되며, Low 결함까지 매번 5단계로 되돌리면 파이프라인 처리량이 저하된다(과잉 대응 방지). 코드 수정 자체는 워크플로 소유자(5단계)의 판단 영역이라 6단계가 직접 고치지 않고, `unit-10-test.md` §6/§7에 재현 절차·근거·권고 수정안(`[ -z "${value// }" ]` 또는 값 트리밍)을 남겨 10~11단계(운영 Runbook)로 승계했다 | 에이전트 자체판단 (근거: `unit-10-test.md` TC-005d 실측, `set -euo pipefail`이 만드는 방어선 분석, 규칙F의 심각도 비례 대응 원칙) | Low (워크플로 스크립트 3줄 미만 수정으로 해소 가능한 경계값 보강이며, 기존 데이터/스키마/시크릿 체계에 영향 없음) | `docs/harness/units/unit-10-test.md`(DEF-001), `.github/workflows/neon-db-backup.yml`(향후 보강 대상, 이번 결정으로는 미수정) |
| DEC-035 | 2026-09-17 | 하네스 자체(전 단계 공통) | WU-08/WU-09 진행 중 사용자가 `TaskStop`으로 작업을 강제 중지시킨 3건(`.venv_edge`, `.venv_wu09_test`, `.venv_wu09_it`)에서, 검증용 venv/DB가 프로젝트 루트에 그대로 남아 매번 사용자가 직접 `find`/`git status`로 확인 후 손수 삭제해야 했다. 원인 분석 결과 하네스 자체에 "정상 종료 시 마지막 단계"에만 의존하는 정리 로직만 있고, 강제 중단 시 정리를 보장하는 설계가 없었다(구조적 결함) — `.gitignore`도 부재했다. 하네스(ORCHESTRATOR.md/templates/automation) 자체를 개선할지, 범위를 어디까지로 할지 결정이 필요했다 | 사용자에게 4개 항목(①신규 전역 규칙 ②`.gitignore` 신설 ③테스트결과서 Teardown 섹션 ④janitor 자동점검 스크립트)에 대해 AskUserQuestion으로 한 번에 확인받아 전원 권장안(전부 채택)으로 승인받았다. **규칙 K(중단-안전 정리)**를 신설해 (1) 검증용 venv/임시 DB/설정은 `.harness-tmp/`에만 생성 (2) 정상 종료 시 정리 확인이 PASS의 필요조건 (3) 강제 중단 시 다음 상호작용 시작 때 재점검 (4) `automation/harness-janitor.sh`로 점검을 사람 손이 아닌 스크립트로 강제하도록 했다. `.gitignore` 신설, `test-report-template.md`에 "7절 테스트 환경 정리(Teardown)" 신설(PASS 필요조건화), `.claude/agents/06·07·08·10-*.md`에 규칙 K 반영, `automation/run-harness-agent.sh`·`README.md` 갱신, `automation/git-hooks/pre-commit.sample` 신설도 함께 적용했다. 이 결정은 진행 중인 WU-08/09/10의 기존 산출물을 소급 무효화하지 않는다(과거 유닛 재작업 불필요 — 앞으로의 신규 검증부터 적용) | 사용자 응답 (AskUserQuestion 4문항, 4개 항목 모두 "권장" 옵션 채택) | Medium (템플릿/에이전트 정의에 새 절/규칙이 추가되는 구조 변경이지만, 기존 코드·데이터·완료된 산출물에는 영향 없고 텍스트 되돌리기로 즉시 원복 가능) | `ORCHESTRATOR.md`, `CLAUDE.MD`, `.gitignore`(신규), `templates/test-report-template.md`, `.claude/agents/06-unit-tester.md`, `.claude/agents/07-integration-tester.md`, `.claude/agents/08-full-system-tester.md`, `.claude/agents/10-deploy-tester.md`, `automation/run-harness-agent.sh`, `automation/README.md`, `automation/harness-janitor.sh`(신규), `automation/git-hooks/pre-commit.sample`(신규) |
| DEC-036 | 2026-09-17 | 하네스 자체(전 단계 공통) | 마스터 하네스(HANESS_AUTO)에서 "바이브코딩 하네스가 2~3일 걸린다"는 사용자 피드백에 따라 프로젝트 위험도 등급(Tier) 시스템 + 독립 작업 단위 병렬 처리 + Low등급 06·07 병합 개선이 도입됨. 이 프로젝트(AI-AUTO-WORK)는 이미 최신 13단계 구조와 100% 동일한 상태였으므로(ORCHESTRATOR.md·템플릿·에이전트 정의가 마스터와 바이트 단위로 일치하는 것을 diff로 직접 확인) 이 개선사항을 그대로 반영할지, 반영한다면 이 프로젝트의 Tier를 무엇으로 선언할지 결정이 필요했다 | 개선사항을 그대로 반영(`ORCHESTRATOR.md`, `templates/verification-log-template.md`, `templates/test-report-template.md`, `.claude/agents/06-unit-tester.md`, `.claude/agents/07-integration-tester.md`를 마스터 최신본으로 동기화). 이 프로젝트의 Tier는 **Standard**로 선언한다 — 근거: 회원가입/결제/규제 데이터 없음(DEC-005), AI/LLM 생성 기능도 명시적으로 비해당 확인됨(REQ-025, 02-planning.md). Standard는 규칙B(2회 검증)·06/07 분리가 원문 그대로 유지되므로, 이미 완료된 WU-01~10의 검증 방식·산출물은 전혀 영향받지 않는다(소급 재작업 없음). 앞으로 신규 작업 단위가 생기면 이 Tier 기준으로 진행한다 | 사용자 응답 (AskUserQuestion 2문항 — STOCK-ANALYZER-KR 업그레이드 범위, 이 프로젝트의 Tier) | Low (Tier 선언 자체는 라벨링 수준이며 Standard는 기존 규칙 강도와 동일 — 되돌리기 매우 쉬움) | `ORCHESTRATOR.md`, `templates/verification-log-template.md`, `templates/test-report-template.md`, `.claude/agents/06-unit-tester.md`, `.claude/agents/07-integration-tester.md` |

## 작성 규칙
- **비가역성** 컬럼: High(되돌리기 매우 어려움: DB 스키마 확정, 프레임워크 선택, 인증 방식 등) / Medium / Low로 표기한다.
- "에이전트 자체판단"으로 기록하는 경우, 반드시 판단 근거를 결정 내용에 함께 서술한다. 근거 없는 자체판단 기록은 무효로 간주하고 규칙 A에 따라 사용자에게 확인받아야 한다.
- 규칙 F(피드백 루프)에 의해 재작업이 발생한 경우, 원래 결정을 수정하는 새 항목을 추가하고 이전 항목을 참조한다 (예: "DEC-004는 DEC-001을 대체함, 사유: 9단계 보안검증에서 인증모델 결함 발견").
