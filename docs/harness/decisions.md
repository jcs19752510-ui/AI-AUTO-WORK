# 의사결정 로그 (Decision Log)

> 규칙 A(모르면 질문)에 따라 사용자에게 질문하고 답을 받았거나, 규칙 A-3(비가역적 결정)에 따라 에이전트가 근거를 갖고 스스로 결정을 내린 경우 반드시 여기에 append한다. 절대 삭제/수정하지 않고 append-only로 유지한다 (과거 결정의 맥락을 잃지 않기 위함).

| ID | 일시 | 단계 | 질문/이슈 | 결정 내용 | 결정자 (사용자 응답 / 에이전트 자체판단) | 비가역성 | 영향받는 산출물 |
|----|------|------|-----------|-----------|------------------------------------------|----------|------------------|
| DEC-001 | 2026-09-16 | 0 (사전절차) | 프로젝트에 MCP(GitHub/GitLab, Playwright/Chrome, Sentry/Datadog, Slack/Teams 등)를 연동할지 여부 (ORCHESTRATOR.md §4-3 필수 절차) | 연동 안 함. 하네스 전 단계를 MCP 없이 기존 방식(코드/문서/Bash 조사, 대화창 텍스트 질문·승인)으로 진행 | 사용자 응답 | Low | 전 단계 공통 (2·8·10·11·12·13단계에서 MCP 대체 수단을 결과서에 명시) |
| DEC-002 | 2026-09-16 | 0 (사전절차) | 신규 사이트의 목적/유형 | 블로그/콘텐츠 사이트 | 사용자 응답 | Medium | 01-trend-analysis, 02-planning |
| DEC-003 | 2026-09-16 | 0 (사전절차) | 기존 준비 자산(디자인 시안/콘텐츠/도메인) 유무 | 없음 — 전부 새로 기획/디자인 | 사용자 응답 | Low | 02-planning, 04-ux-design |
| DEC-004 | 2026-09-16 | 0 (사전절차, 규칙A-③ 비가역적 결정) | 최초 요청은 "워드프레스 신규 사이트를 Render 서버로 구축"이었으나, Render는 관리형 MySQL을 제공하지 않고 워드프레스는 MySQL/MariaDB 전제로 설계되어 PostgreSQL(Neon)과 직접 호환되지 않음(비공식 어댑터인 PG4WP는 플러그인 호환성 리스크가 커 실무 비권장). 이를 사용자에게 알리고 방향 선택을 요청함 | **워드프레스를 포기**하고, PostgreSQL(Neon) + Python 스택으로 기능적으로 동등한 블로그/콘텐츠 사이트를 새로 구축하기로 결정. 배포는 계속 Render 사용. 구체적 Python 프레임워크/CMS 선택(예: Django+Wagtail 등)은 3단계 설계자가 근거와 함께 제안하고 decisions.md에 추가 기록 | 사용자 응답 | High (프레임워크/DB 엔진 전체 교체 — 되돌리기 매우 어려움) | 01-trend-analysis 이하 전 단계 (특히 02-planning, 03-system-design) |
| DEC-005 | 2026-09-16 | 2 (기획서, 규칙A-③ 비가역적 결정) | 사이트 수익화 모델(무료/광고/유료구독) — 02번 기획서 작성 중 에이전트가 규칙A에 따라 질문 목록으로 제기함(선택지마다 데이터 모델·PG 보안범위·개인정보 고지범위가 실질적으로 달라짐) | **완전 무료** (v1 계획대로 진행). 광고/결제 연동 없음. 02-planning.md에 Out-of-Scope로 표시된 REQ-022~024, WU-12(수익화 작업단위)는 계속 보류 상태로 유지 — 추후 별도 요청 시에만 규칙F에 따라 재작업 | 사용자 응답 | High (수익화 모델 변경은 데이터모델·보안범위·법적고지 전반에 영향) | 02-planning.md, 03-system-design (수익화 관련 설계 범위 제외 근거) |
| DEC-006 | 2026-09-16 | 3 (설계, 규칙A-③ 비가역적 결정) | 01 §3-1에서 제시된 Python CMS 후보(Wagtail vs Django-CMS) 중 최종 프레임워크를 3단계가 확정해야 함(DEC-004에서 위임됨) | **Wagtail(Django 5.2 LTS 기반, Wagtail 7.4 LTS 계열로 버전 고정)을 최종 채택.** 근거: (1) PostgreSQL 네이티브 지원, (2) Render 공식 Django 가이드 + Wagtail 전용 배포 사례 다수(01 §3-1, render.com/docs/deploy-django 2026-09-16 재확인), (3) 공식 릴리스 노트(docs.wagtail.org 2026-09-16 직접 조회) 확인 결과 LTS가 7.0→7.4로 계속 갱신되며 유지보수가 활발함이 재확인됨, (4) Wagtail의 Page 리비전/임시저장/예약발행 내장 기능이 REQ-001과 정확히 일치. Django-CMS는 LTS/릴리스 정보를 이번 조사로도 재확인하지 못해("확인 필요") 불확실한 근거로 비가역적 결정을 내리지 않는다는 원칙에 따라 기각. 최신 8.0(비LTS) 대신 7.4 LTS를 고정한 이유는 1인/소규모 운영(가정 A1)에 맞춰 유지보수 안정성을 유행보다 우선했기 때문. 라이선스 BSD-3-Clause(github.com/wagtail/wagtail LICENSE 직접 확인) 확인 완료, 상용/배포와 충돌 없음 | 에이전트 자체판단 (근거: 03-system-design.md §2.1) | High (프레임워크/CMS 선택 — 되돌리기 매우 어려움) | 03-system-design.md §1~§5, 이후 04~08단계 전체 |
| DEC-007 | 2026-09-16 | 3 (설계, 규칙A-③ 비가역적 결정) | 콘텐츠 데이터의 핵심 스키마 구조(콘텐츠 CRUD/편집워크플로를 어떤 모델 구조로 구현할지)를 확정해야 함 | Wagtail의 **Page 트리 상속 모델**(`BlogPostPage`가 Wagtail `Page`를 상속)을 콘텐츠 핵심 스키마로 채택하고, 임시저장·예약발행(`go_live_at`/`expire_at`)·리비전 이력을 Wagtail 코어가 제공하는 `PageRevision` 메커니즘 그대로 사용(커스텀 재구현 안 함). Category/Tag는 별도 Snippet/M2M(django-taggit 패턴)으로 분리. 근거: DEC-006(Wagtail 채택)에 직접 종속된 구조적 귀결이며, REQ-001을 프레임워크 표준 기능으로 충족해 커스텀 개발 리스크를 최소화하기 위함 | 에이전트 자체판단 (근거: 03-system-design.md §3) | High (핵심 스키마 확정 — 되돌리기 매우 어려움) | 03-system-design.md §3, 05단계(WU-02) 구현 |
| DEC-008 | 2026-09-16 | 3 (설계, 규칙A-③ 비가역적 결정) | REQ-006(이미지/미디어 업로드용 S3 호환 오브젝트 스토리지)의 구체적 벤더를 확정해야 함(01 §8-3에서 3단계로 위임) | **Cloudflare R2를 채택.** 후보 AWS S3, Neon Object Storage(베타)와 공식 가격문서(전부 2026-09-16 직접 조회) 기준으로 비교: R2는 이그레스(다운로드) 비용이 영구 무료이고 무료 티어(저장10GB/Class A 100만회/Class B 1000만회)가 제공되며 "프로덕션 비권장" 문구가 없는 정식 GA 제품인 반면, S3는 이그레스 과금이 있어 무료·비영리 MVP(DEC-005)와 비용구조가 맞지 않고, Neon Object Storage는 베타 단계라 DB·스토리지 장애 지점을 한 벤더에 몰아넣게 되어 장애 격리 원칙에 반함. `django-storages`(BSD-3-Clause, github.com/jschneier/django-storages LICENSE 직접 확인) + boto3의 S3 호환 엔드포인트로 연동. 버킷은 `media-public`(공개)/`backup-private`(비공개, DEC-010과 연계)로 분리 | 에이전트 자체판단 (근거: 03-system-design.md §2.3) | High (오브젝트 스토리지 벤더 확정 — 이미지 URL 스킴이 이 벤더에 종속되어 이후 마이그레이션 비용이 큼) | 03-system-design.md §2.3, §3.2, §5.5, WU-03 |
| DEC-009 | 2026-09-16 | 3 (설계, 규칙A-③ 비가역적 결정) | 인증/인가 모델(운영자·에디터 vs 구독자)을 확정해야 함(02 REQ-010) | **Django 세션 기반 인증 + Wagtail 그룹 기반 페이지 권한**을 운영자/에디터 인증 모델로 채택(OAuth/SSO/JWT 도입 안 함). 구독자(REQ-016)는 계정을 만들지 않고 이메일만 수집하는 무인증 모델로 확정. 어드민 마운트 경로를 기본값(`/admin/`)에서 변경해 자동화 공격 노출을 줄임. 근거: 1인/소규모 운영(가정 A1)에서 외부 IdP 연동은 과설계이며, 회원제(REQ-024)가 Out-of-Scope이므로 구독자용 인증 체계 자체가 불필요 | 에이전트 자체판단 (근거: 03-system-design.md §5.1) | High (인증 방식 확정 — 되돌리기 어려움, 이후 회원제 도입 시 재설계 필요) | 03-system-design.md §5.1, §4, WU-09 |
| DEC-010 | 2026-09-16 | 3 (설계) | Neon 무료 PITR(6시간) 한계를 보완할 백업 메커니즘(REQ-013)을 구체적으로 설계해야 함 | **GitHub Actions 스케줄 워크플로(1일 1회)가 `pg_dump`를 실행해 Cloudflare R2의 `backup-private` 버킷에 업로드**하는 방식으로 확정. Render Cron Jobs는 공식 문서(render.com/docs/free, 2026-09-16 확인) 기준 무료 티어로 배포 가능한 서비스 유형에 포함되지 않아(Web Service/Postgres/Key Value만 무료) 제외. GitHub Actions를 쓰면 앱(Render) 장애와 백업 실행이 서로 독립적으로 동작해 장애 격리가 되고, 별도 유료 서비스 없이 무료로 구현 가능. R2 Object Lifecycle 정책으로 14일 후 자동 만료 | 에이전트 자체판단 (근거: 03-system-design.md §1.1, §3.3, §7.3) | Medium (백업 메커니즘 자체는 나중에 교체 가능하나, 데이터 복구 가능성에 직접 영향을 주는 안전장치이므로 신중히 기록) | 03-system-design.md §1.1/§3.3/§7.3, WU-10 |
| DEC-011 | 2026-09-16 | 3 (설계) | Render 무료 티어 콜드스타트(REQ-011) 대응 방식으로 "킵얼라이브 핑을 도입할지 여부"를 확정해야 함 | **킵얼라이브 핑을 v1에서 도입하지 않기로 결정.** 15분 간격 핑으로 스핀다운을 막으면 월 750 무료 인스턴스시간(공식 확인, render.com/docs/free)의 대부분(약 720시간)을 소진해 향후 추가 무료 서비스 여유가 사라짐. Render가 스핀업 중 자체 로딩 페이지를 표시하는 공식 기능을 1차 방어선으로 삼고, 가끔의 콜드스타트 UX 저하는 무료·비영리·저트래픽 MVP(DEC-005) 단계에서 의도적으로 수용. §6.3에 명시된 4가지 기준 중 하나라도 충족 시 유료 Starter 플랜(스핀다운 없음)으로 전환 | 에이전트 자체판단 (근거: 03-system-design.md §6.3) | Medium (운영 정책 수준 결정이며, 유료 전환으로 비교적 쉽게 뒤집을 수 있으나 비용/UX 트레이드오프가 크므로 기록) | 03-system-design.md §6.3, §8, 운영 Runbook(11단계) |
| DEC-012 | 2026-09-16 | 3 (설계) | 캐싱 전략(Neon 무료 컴퓨트시간 보호 목적)을 확정해야 함 | **v1은 Django LocMemCache 기반 뷰 캐시(5~15분 TTL)만 사용**하고, Redis/Render Key Value(무료 제공되나 별도 서비스) 도입은 v1 범위에서 제외. 근거: 현재 트래픽 규모(월 500UV 목표, 02 §5)에서 분산 캐시 도입 이득 대비 추가 서비스/장애지점 운영 복잡도가 크다고 판단(과설계 방지 원칙). 트래픽이 실제로 늘어 재검토가 필요해지면 그때 도입 | 에이전트 자체판단 (근거: 03-system-design.md §2.5) | Low (설정 수준 결정, 언제든 Redis로 교체 가능) | 03-system-design.md §2.5, §6.1 |

| DEC-013 | 2026-09-16 | 5 (개발, WU-01) | Wagtail/Django 프로젝트 골격을 저장소 어디에 배치할지 — 표준 `wagtail start`가 프로젝트 루트에 생성하는 `templates/`(base.html 등)가 이 하네스가 이미 저장소 루트에서 쓰고 있는 `templates/`(decision-log-template.md 등 공통 산출물 양식)와 이름이 충돌함. 02/03/04번 어디에도 정확한 배치 경로가 명시되지 않았음 | Django/Wagtail 프로젝트 전체(`manage.py`, `config/`(설정 패키지), `home/` 등)를 저장소 루트가 아니라 **`webapp/` 하위 디렉터리**에 배치하기로 결정. 설정 패키지명은 Wagtail 공식 예시와 동일한 `config`를 사용 | 에이전트 자체판단 (근거: 폴더명 충돌 회피가 목적이며, 두 갈래 해석이 결과를 크게 바꾸는 비가역적 결정은 아님) | Low (폴더 이름 변경만으로 되돌릴 수 있음) | webapp/ 이하 전체 구조. 향후 WU-02~WU-11이 이 경로(`webapp/home`, `webapp/config` 등)를 기준으로 앱을 추가함 |
| DEC-014 | 2026-09-16 | 5 (개발, WU-01) | 03-system-design.md §2.1에서 "확인 필요"로 남긴 항목 — Wagtail 7.4가 Django 5.2 LTS만 지원하는지, 5.2/6.x를 함께 지원하는지를 WU-01 착수 시 재확인하라고 명시되어 있었음 | `pip download`로 Wagtail 7.4.3의 실제 PyPI 배포 메타데이터(`Requires-Dist: Django>=5.2`, 상한 버전 명시 없음)를 직접 조회해 확인. Django 5.2 LTS와 호환됨이 실측으로 재확인되어 DEC-006의 "Django 5.2 LTS 고정" 방침을 그대로 유지할 수 있음. 다만 상한이 없어 `pip install`만 하면 최신 Django(6.1.1)가 끌려올 수 있으므로, `requirements.txt`에 `Django==5.2.17`을 명시적으로 고정해 LTS 고정 의도가 실제로 지켜지도록 함 | 에이전트 자체판단 (근거: `pip index versions`/`pip download` 실측 조회, unit-01-note.md 게이트2 체크리스트) | Low (버전 핀은 언제든 조정 가능하며 스키마/아키텍처에 영향 없음) | webapp/requirements.txt, 03-system-design.md §2.1 확인 필요 항목 해소(문서 자체는 별도 갱신하지 않고 이 로그로 추적) |
| DEC-015 | 2026-09-16 | 3 (설계, 규칙F 재작업) | 07단계 통합테스터(`feature-WU-01-integration-test.md`, FAIL 판정)가 WU-01을 실제 배포 형태(WSGI+미들웨어+실제 HTTP 요청)로 조립 검증한 결과 DEF-001(Critical)을 발견함 — production 배포 시 `SECURE_PROXY_SSL_HEADER` 미설정으로 `SECURE_SSL_REDIRECT=True`와 결합해 무한 HTTPS 리다이렉트 루프가 발생, 사이트가 100% 접근 불가 상태가 됨. 근본 원인은 03-system-design.md §5.5가 "Render는 로드밸런서에서 TLS를 종료하고 앱에는 평문 HTTP로 전달한다"는 배포 환경 전제를 명시하지 않았던 것(규칙F-1: 근본 원인 단계까지 소급). 같은 카테고리(리버스프록시 뒤 흔한 누락 설정)도 전수 점검이 필요했음 | 03-system-design.md §5.5를 v1.2로 갱신: ① Render TLS 종료 아키텍처를 배포 환경 전제로 명시(§5.5.1), ② `SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")`를 필수 설정으로 정식 추가하고 무한루프 인과관계 명시(§5.5.2), ③ `USE_X_FORWARDED_HOST`는 점검 결과 확인 완료·조치 불필요로 판정(원본 Host 헤더가 변경 없이 전달됨을 IT-04로 실측 확인), ④ `CSRF_TRUSTED_ORIGINS`는 이미 WU-01 구현에 올바르게 존재하던 것을 정식 요구사항으로 격상, ⑤ `X-Forwarded-For` 기반 클라이언트 실제 IP 처리가 완전히 누락되어 있음을 신규 발견(§5.1 로그인 레이트리밋/§5.3 스팸 IP 마스킹의 실효성을 무력화하는 잠재 결함, Medium)하고 5단계 구현 시 반영할 조치 요구사항을 §5.5.4에 명시. 규칙B 내부검증 2회를 이번 재작업 범위에 대해 처음부터 재수행(verify-log_03-system-design.md "재작업 라운드" 참고) | 에이전트 자체판단 (근거: feature-WU-01-integration-test.md IT-02/IT-03/IT-04 실측 결과 + Render 공식 배포 아키텍처) | Medium (설정 값 수정 자체는 되돌리기 쉬우나, 보안(스푸핑 신뢰 경계)·가용성(무한루프로 인한 전면 장애)에 직접 영향을 주므로 신중히 기록) | 03-system-design.md §5.5/§8/§10, 이후 5단계(WU-01 `config/settings/production.py` 코드 수정), 6단계(`unit-01-test.md` 재검증), 7단계(`feature-WU-01-integration-test.md` 재실행) |

## 작성 규칙
- **비가역성** 컬럼: High(되돌리기 매우 어려움: DB 스키마 확정, 프레임워크 선택, 인증 방식 등) / Medium / Low로 표기한다.
- "에이전트 자체판단"으로 기록하는 경우, 반드시 판단 근거를 결정 내용에 함께 서술한다. 근거 없는 자체판단 기록은 무효로 간주하고 규칙 A에 따라 사용자에게 확인받아야 한다.
- 규칙 F(피드백 루프)에 의해 재작업이 발생한 경우, 원래 결정을 수정하는 새 항목을 추가하고 이전 항목을 참조한다 (예: "DEC-004는 DEC-001을 대체함, 사유: 9단계 보안검증에서 인증모델 결함 발견").
