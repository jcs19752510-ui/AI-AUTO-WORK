# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/01-trend-analysis.md`
- 작성 에이전트: `01-trend-analyst`
- 버전: v2 (최종, 1차 검증에서 발견된 결함 수정 반영)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 20년차 기획자 겸 시장 분석가 (작성자 본인, 자가 재검토)
- 일시: 2026-09-16
- 체크리스트
  - [x] 입력 계약(Input Contract)에 명시된 모든 입력을 실제로 반영했는가 — 조사 대상 도메인(블로그/콘텐츠 사이트), 타깃 사용자층(미정 상태를 그대로 반영), 사용자가 지정한 5개 중점 조사 항목(일반 시장동향/경쟁환경, Python CMS 프레임워크 비교, Render+Neon 배포 패턴, 법적 이슈, LLM 해당없음)을 모두 반영함
  - [x] 출력 계약(Output Contract)에 명시된 모든 필수 항목이 빠짐없이 존재하는가 — 8개 섹션(조사범위/시장동향/경쟁환경/타깃니즈/규제/도메인운영규칙/리스크/시사점) 전부 작성됨. "해당 없음" 처리한 항목(규칙 I, 규칙 J)에는 사유를 명시함
  - [x] 상위 단계 산출물과 용어/수치/범위가 모순되지 않는가 — `decisions.md`의 DEC-001~DEC-004(MCP 미연동, 블로그/콘텐츠 사이트, 기존 자산 없음, 워드프레스 포기·Python+Neon 전환)와 용어/제약이 일치함을 확인
  - [x] 추측으로 채운 항목이 있는가 (있다면 "가정" 섹션에 명시했는가, 혹은 질문으로 전환했는가) — Wagtail이 Django-CMS보다 "용도상 부합해 보인다"는 서술은 트렌드분석가의 추정임을 §3-1 결론에 명시적으로 표기하고, 최종 결정 권한이 3단계 설계자에게 있음을 명확히 함. 그 외 확인 못한 항목은 전부 "확인 필요"로 표기함(§5, §7)
  - [x] 20년차 실무자 기준으로 봤을 때 구조적/논리적 결함이 없는가 — **최초 초안에서 결함 1건 발견**: 사용자가 특별 요청한 항목 2번("Python 생태계에서 워드프레스에 대응하는 CMS/프레임워크 선택지 비교, PostgreSQL 지원/유지보수 활발도/Render 배포 용이성 관점 비교")이 시장 동향 섹션(§2)에 산발적으로만 언급되고 독립된 비교 절이 없었음. 이 자료는 "3단계 설계자가 프레임워크를 선택할 때 근거로 쓸 자료이므로 중요"하다고 사용자가 명시했는데, 구조적으로 찾기 어렵게 배치되어 있었음
  - [x] 오탈자, 형식 오류, 누락된 표/그림 참조가 없는가 — 마크다운 표 구조, 헤더 레벨, 출처 링크 형식 확인함. 이상 없음
- 발견된 결함 목록:
  1. §3(유사 서비스/경쟁 환경)에 Python CMS 프레임워크(Wagtail vs Django-CMS vs Node 기반 Directus/Strapi/Payload) 비교가 독립된 절로 정리되어 있지 않음 — 3단계 설계자가 참고자료로 바로 활용하기 어려운 구조였음
- 조치 내용: (수정 후 → v1) §3을 "3-1. Python CMS/블로그 프레임워크 후보 비교"와 "3-2. 일반 경쟁 서비스 비교"로 분리. 3-1에 Wagtail/Django-CMS/Directus/Strapi/Payload 5개 후보를 PostgreSQL 지원·유지보수 활발도·Render 배포 용이성·비고 기준으로 비교하는 표를 추가하고, Node 기반 후보가 왜 주 후보에서 제외되는지 명시적 근거를 서술함. §7(리스크)에 "Django-CMS 최신 LTS 정보 미확인"을 8번 항목으로 추가하고, §8(시사점)에 2번 항목으로 "Python CMS 프레임워크 후보와 선택 근거를 3단계로 명확히 인계할 것"을 추가함

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
> "내가 이 문서를 오늘 처음 받아본 심사자라면?" — 2단계 기획서 작성자(02-planning-writer) 관점으로 가정하고 재검토
- 검증자(역할): 20년차 아키텍트/설계자 관점 (2단계 산출물을 실제로 받아쓸 사람의 입장에서 독립 재검토)
- 일시: 2026-09-16
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가 (재확인) — §3-1 신설, §7-8·§8-2 반영 확인함
  - [x] 엣지 케이스/예외 상황이 누락되지 않았는가 — 콜드스타트 중첩(Render 앱 + Neon DB 동시 발생 시 응답 지연), 미디어 파일 영속성 문제(컨테이너 재시작 시 삭제), 무료 PostgreSQL 30일 만료 vs Neon 무상 유지 차이 등 이 도메인/스택 특유의 엣지 케이스가 §6에 반영되어 있음을 확인
  - [x] 문서만 보고 다음 단계 에이전트가 추가 질문 없이 작업을 시작할 수 있는가 — 타깃 사용자층이 미확정이라는 점이 반복적으로(§1, §4, §7-1, §8-1) 명시되어 있어, 2단계 작성자가 "이건 트렌드 분석가가 빠뜨린 게 아니라 의도적으로 2단계로 넘긴 것"임을 명확히 인지할 수 있음. 프레임워크 선택도 마찬가지로 "3단계 권한"임이 명시되어 있어 2단계 작성자가 임의로 확정하지 않도록 경계를 명확히 그어줌
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거가 명시되어 있는가 — DEC-004(워드프레스 포기, Python+Neon 전환)의 근거가 §3-2 표(MySQL 전제 vs Neon 비호환)에서 재확인됨. §3-1 결론에서 "최종 프레임워크 결정과 근거는 3단계 설계자가 별도로 내리고 decisions.md에 기록해야 한다"고 명시해 규칙 G 준수를 다음 단계에 정확히 인계함
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — 오브젝트 스토리지 미연동 시 미디어 파일 소실 리스크, Neon 무료 티어 PITR 6시간 제한으로 인한 데이터 손실 복구 불가 리스크가 명시적으로 경고되어 있음. 추가로 재검토한 결과, 규제 판단(§5, 부록)에서 "인허가 대상 아님"이라는 결론을 에이전트가 내렸지만, 이는 되돌리기 쉬운 판단(콘텐츠 주제가 정해지면 언제든 재검토 가능하다고 §5·§7-1에 명시)이므로 규칙 I 위반이 아님을 확인
- 발견된 결함 목록: 없음 (2차 검증 결과 추가 결함 0건)
- 조치 내용: 없음 — v1을 그대로 v2(최종)로 확정

## 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
- [ ] FAIL — 재작업 필요, 사유:

## 비고
- 검증 2회 모두 결함 발견 프로세스를 실제로 거쳤음: 1차에서 구조적 결함 1건을 발견·수정했고, 2차는 수정 사항 재확인 및 독립 관점 추가 점검에서 결함 0건으로 확인되어 규칙 B("2회는 하한선")를 충족함.
- 미해결로 남겨둔 항목(타깃 사용자층 미확정, 규제 세부사항 확인 필요, WordPress 점유율·AI 트래픽 감소폭 수치 상충 등)은 은폐하지 않고 §7(리스크 및 불확실성)에 전부 명시적으로 남겼으며, 이는 결함이 아니라 조사 범위/시점의 본질적 한계이므로 PASS 판정에 영향을 주지 않는다고 판단함.
