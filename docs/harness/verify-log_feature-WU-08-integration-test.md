# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/feature-WU-08-integration-test.md`
- 작성 에이전트: `07-integration-tester`
- 버전: v0 (초안)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): `07-integration-tester` (작성자 본인, 자가 재검토)
- 일시: 2026-09-17
- 체크리스트
  - [x] 입력 계약(Input Contract)에 명시된 모든 입력을 실제로 반영했는가 — `unit-08-note.md`(§1~§8), `unit-08-test.md`(AC1~19/TC-001~029, PASS), `verify-log_unit-08-test.md`(2회 검증 PASS), `decisions.md` DEC-026/DEC-027, `feature-WU-07-integration-test.md`(직전 회귀 기준선), `03-system-design.md` §6.3/§7.2/§7.4를 전부 읽고 각 산출물이 주장한 내용을 코드(웹앱 실제 파일)와 대조했다.
  - [x] 출력 계약에 명시된 모든 필수 항목이 빠짐없이 존재하는가 — `test-report-template.md`의 9개 섹션(1~9) + traceability.md 갱신(§10) 전부 채웠다. "해당 없음" 처리한 섹션은 없다.
  - [x] 지시사항이 명시한 "단위 간 데이터 흐름/상태 전이", "회귀", "업무 단위 수준 E2E"가 각각 케이스로 매핑됐는가 — §5 매핑표로 재확인. 특히 지시사항이 명시적으로 지목한 4개 경계(기존 캐시 전략/CSRF 처리/XForwardedForMiddleware/뉴스레터 폼/legal 페이지/sitemap·SEO)를 §4-2~§4-5가 전부 실측 케이스로 다뤘는지 하나씩 대조했다 — 누락 없음.
  - [x] 이미 06단계가 검증한 것을 반복하지 않았는가(규칙B) — §2 제외범위에 06단계가 이미 PASS로 확정한 AC1~19/TC-001~029 목록을 명시하고, 이번 07단계 케이스(IT-01~15)가 전부 "WU-01~07과 조립됐을 때"라는 새 경계에서만 설계됐는지 재확인. 예외적으로 IT-01~03/IT-06/IT-08은 06단계와 유사해 보이지만, 실제로는 (a) 신규 venv로 처음부터 재현해 회귀 기준선을 재확립하거나(IT-01~03), (b) 06단계가 다루지 않은 실제 요청 처리 흐름(레이트리밋 경계값이 WU-08 미들웨어 삽입 이후에도 유지되는지, IT-08)과 결합해 검증한 것이므로 단순 반복이 아님을 재확인했다.
  - [x] 추측으로 채운 항목이 있는가 — 없음. 모든 "실제 결과"는 이번 세션에서 직접 실행한 Django `test.Client()` 요청/응답, `core.monitoring.get_usage_snapshot()` 조회, `django.core.mail.outbox` 조회 결과를 그대로 옮겼다.
  - [x] 20년차 QA 리드 기준으로 구조적 결함이 없는가 — 개별 단위테스트가 서로 다른 요청으로 독립 검증한 "모니터링 카운팅"과 "AdminEmailHandler 알림"을 이번 07단계가 **같은 미처리 500 예외**에서 동시에 재현해(IT-09/10) 단위 간 조립 지점의 가치를 실제로 창출했는지 재확인 — 이 관점이 이 업무 단위 통합테스트의 핵심 존재 이유이므로 특히 엄격히 점검했다.
  - [x] 오탈자/형식 오류/누락된 표 참조가 없는가 — 표 번호(IT-01~15)와 본문 참조가 §4/§5/§6/§7/§8에서 일관되는지 재확인. 초안에서 §6 DEF-001의 ID가 이미 `feature-WU-01-integration-test.md`가 쓰던 "DEF-001"과 동일 명칭이라 향후 문서 간 참조 시 혼동 가능성을 발견했다.
- 발견된 결함 목록:
  1. §6 결함 목록의 신규 발견 항목 ID를 `DEF-001`로 붙였는데, 이는 이미 WU-01 통합테스트(`feature-WU-01-integration-test.md`)가 "무한 리다이렉트" 결함에 쓴 ID와 동일 문자열이라 동일 문서군에서 혼동될 수 있다.
- 조치 내용: (수정 후 → v1) 이번 문서 §6의 신규 발견 항목 ID를 `DEF-WU08IT-01`로 변경해 다른 WU의 `DEF-001`과 명확히 구분되도록 했다. §7/§8/§10 등 다른 절에서 이 ID를 인용한 곳이 있는지 전수 검색해 함께 정정했다.

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
> "내가 이 문서를 오늘 처음 받아본 심사자라면?"이라는 관점에서, 1차 검증자가 놓쳤을 법한 리스크·엣지케이스·암묵적 가정을 의심하며 재검토한다. 특히 지시사항이 요구한 "이 업무 단위가 다른 업무 단위와 만나는 지점(8단계 전체 테스트)에서 문제가 생기지 않을까"를 의심한다.
- 검증자(역할): `07-integration-tester` (독립 심사자 관점으로 역할 전환, `08-full-system-tester`를 가정한 재검토)
- 일시: 2026-09-17
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가(재확인) — `DEF-WU08IT-01`로 ID가 일관되게 바뀌었는지 §6/§7/§8/§10을 재검색해 확인. 잔재 없음.
  - [x] 엣지 케이스/예외 상황이 누락되지 않았는가 — 검증 스크립트 작성 과정에서 IT-15(대시보드 혼합 트래픽)가 최초 1회 실패했던 경위(스크립트 자체 버그, 제품 결함 아님)를 §4-7에 투명하게 남겼는지 재확인 — 남아 있음. "처음엔 실패했다"는 사실을 숨기지 않는 것이 규칙C 정신에 부합한다고 판단해 그대로 유지했다.
  - [x] 문서만 보고 8단계 담당자가 추가 질문 없이 작업을 시작할 수 있는가 — §7 리스크 절이 8단계가 참고할 잔존 이슈(정적자산 카운팅 혼입/SMTP 미검증/healthCheckPath 미검증/--workers 1 미실측/XFF 공식보증/HTTPS CSRF 브라우저 미검증/wagtail_hooks docstring 불일치)를 전부 승계 상태와 함께 명시했는지 확인 — 충분함.
  - [x] **8단계(전체 풀테스트)에서 이 업무 단위가 다른 업무 단위와 만나는 지점에서 문제가 생기지 않을까** —
    1. WU-08이 WU-09(관리자 인증/권한, REQ-010) 착수 시 영향을 줄 구조적 이유가 있는가 — `superuser_required`/Wagtail `require_admin_access` 이중 계층이 이미 검증됐고(06단계 TC-008/026), WU-09가 관리자 인증 모델을 확장하더라도 `usage_dashboard`는 이미 superuser 전용으로 최소 권한 원칙을 지키고 있어 구조적 충돌 소지가 낮다는 점을 재확인. 다만 WU-09가 로그인 페이지/리다이렉트 URL(`wagtailadmin_login`)을 변경하면 `core/views.py::superuser_required`의 `login_url` 하드코딩이 영향받을 수 있다는 점을 신규로 인지해, 8단계/WU-09 개발자에게 참고 사항으로 남긴다(신규 리스크는 아니며, WU-09가 아직 미착수이므로 지금 코드를 고치지 않는다 — 규칙F 대상 아님, 단순 인계).
    2. WU-08이 REQ-012 대시보드에서 노출하는 "요청 수" 지표가 8단계의 전체 시스템 회귀/부하 관점 테스트와 상호작용하는가 — 8단계가 실제로 부하 테스트를 수행하면 `RequestMetricsMiddleware`가 그 트래픽도 그대로 카운트한다(설계상 의도된 동작, §6 DEF-WU08IT-01과 같은 성격). 8단계 담당자가 이 수치 변화를 "결함 징후"로 오인하지 않도록 미리 인지시킨다(신규 리스크 아님, 정보 공유).
    3. §5의 "카테고리 화면 뉴스레터 재검증 생략" 판단(WU-07 통합테스트의 커버리지 확장 근거를 그대로 승계)이 8단계 담당자에게 은폐되지 않고 명시적으로 드러나는가 — §5 커버되지 않은 부분에 명시, 은폐 없음.
    4. 정리(venv/DB/staticfiles/media/임시 설정 모듈/임시 스크립트) 완료와 `git status` 일치 여부 — 재확인, 본문 §3 전제조건/§10(traceability.md 갱신 절 말미의 `git status --porcelain` 확인 문구)의 기술과 실제 정리 결과 일치.
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거가 명시되어 있는가 — 이번 07단계는 신규 비가역적 결정을 내리지 않았다(DEC-026/DEC-027을 재검증만 함). DEF-WU08IT-01을 "코드 수정 대상"이 아니라 "운영자 판단/11단계 인계 대상"으로 분류한 근거(Low 심각도, 기존 caveat 범위 안)를 §6/§7에 명시적으로 남겼는지 재확인 — 남아 있음.
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — IT-07/08에서 production 유사 HTTPS+XFF+레이트리밋 조합이 WU-08 삽입 이후에도 보안 경계(CSRF/레이트리밋)를 지킴을 확인. IT-09/10에서 모니터링 부가기능의 장애(캐시 실패 등, 06단계가 이미 검증)가 실제 요청 처리나 알림 경로를 막지 않음을 다른 각도(미처리 예외 경로)에서 재확인해 가용성 리스크가 없음을 재확인.
- 발견된 결함 목록:
  1. 없음(기능적 결함 기준). WU-09 착수 시 참고할 인계 사항 1건(로그인 URL 하드코딩)을 §9 자체에 기록했으나, 이는 결함이 아니라 사전 정보 공유다.
- 조치 내용: 해당 없음(결함 0건, 추가 조치 불필요) — v1을 최종본으로 확정.

## 최종 판정
- [x] PASS (결함 0건 — Low 심각도 1건은 caveat 범위 내 관찰사항으로 Deferred 처리, 최소 2회 검증 완료) — 다음 단계(8단계)로 handoff 가능
